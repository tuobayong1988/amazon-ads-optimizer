# v665 热修复部署报告

## 1. 概述

本次部署（v665）主要针对生产环境中出现的大账户同步超时、僵尸任务残留以及Elastic Beanstalk（EB）健康检查过于敏感导致频繁重启的问题进行了紧急修复和优化。

**部署状态：**
- **版本号**: v665
- **环境**: amazon-ads-env-prod-v2 (AWS Elastic Beanstalk)
- **状态**: Ready / Green / Ok
- **健康检查**: 数据库连接正常，内存使用正常

## 2. 修复与优化内容

### P0 紧急修复
1. **SP建议竞价步骤超时提升**：将SP建议竞价步骤的超时时间从30分钟提升至60分钟，解决大账户（如90045和90052）在此步骤停滞的问题。
2. **SP否定关键词超时提升**：将SP否定关键词的超时时间从30分钟提升至45分钟，以适应超大账户（5000+ campaigns）的需求。
3. **EB健康检查配置优化**：增加了健康检查的容忍度（UnhealthyThresholdCount从5提升至10），并将Auto Scaling Cooldown从600秒提升至900秒，避免同步期间因CPU/内存短暂飙升导致的误判重启。
4. **SIGTERM信号处理增强**：在进程收到EB重启信号（SIGTERM）时，主动保存所有活跃同步任务的checkpoint，确保重启后能从断点恢复，减少数据丢失和重复同步。

### P1 重要优化
1. **僵尸任务快速检测**：在系统启动时，增加对超过30分钟未更新的残留任务的清理逻辑，并通过与内存中的`activeSyncs` Map进行交叉验证，缩短僵尸任务的清理时间。

### P2 中期优化
1. **竞价查询分批自适应节流**：针对SP建议竞价和商品定位建议竞价，引入了自适应节流策略。对于大账户，初始请求延迟设为1秒；遇到429（Too Many Requests）错误时，延迟加倍（最高8秒）；连续成功5次后，延迟逐步缩减至最低500ms。这有效缓解了API限流问题，提高了大账户的同步成功率。
2. **同步任务资源隔离**：当前系统已处于v659引入的严格串行模式（一次只同步一个账户），天然实现了资源隔离，避免了多个大账户同时同步导致的内存峰值过高问题。

## 3. 验证结果

- **代码编译**：TypeScript编译通过，无新增类型错误。
- **服务启动**：服务成功启动，`/health` 端点返回 `{"status":"healthy","version":"v665"}`。
- **EB环境**：AWS控制台显示环境状态为Green，应用更新成功。

## 4. 后续建议

- **监控增量同步**：在下一个PST凌晨2-3点的自动同步周期，密切监控大账户的同步进度和成功率，验证超时提升和节流优化的实际效果。
- **观察内存使用**：持续观察系统在执行大账户同步时的内存使用情况，确保不会再次触发OOM。
- **Redis队列集成**：按计划推进P2阶段的Redis分布式队列集成，以彻底解决单机内存瓶颈和任务持久化问题。
