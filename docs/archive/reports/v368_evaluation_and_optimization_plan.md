# Amazon Ads Optimizer v368 系统评估与优化方案

## 1. 评估总结

经过对v368版本的全面评估，我们发现系统在核心功能（数据同步、智能优化）方面基本完整，但存在多个严重问题，影响了系统的稳定性、可扩展性和优化效果。主要问题集中在 **API限流机制缺陷、RL模型训练中断、多租户数据隔离不完善、以及前端数据显示不一致** 等方面。此外，当前的 **t3.small实例配置已成为严重瓶颈**，无法支撑200-500租户的规模。

## 2. 问题清单与优先级

| 优先级 | 问题描述 | 模块 | 影响 | 状态 |
|---|---|---|---|---|
| **P0 (紧急)** | API限流服务使用accountId=0，导致所有账户共享限流桶 | `apiRateLimitService` | 数据同步延迟、多租户互相干扰 | **待修复** |
| **P0 (紧急)** | RL奖励回填完全不工作 (totalRLLogs=0) | `rlDataRecorder` | RL模型无法学习，智能优化效果受限 | **待修复** |
| **P0 (紧急)** | 实例配置严重不足 (t3.small, 97%堆内存) | `EB环境配置` | 系统不稳定，易OOM，无法扩展 | **待修复** |
| **P1 (高)** | `tenantMiddleware`是空壳实现，多租户隔离无效 | `tenantMiddleware` | 数据安全风险，配额/功能开关无效 | **待修复** |
| **P1 (高)** | 优化锁冲突反复出现，导致优化指令无法执行 | `lockManager` | 优化指令积压，无法及时生效 | **待修复** |
| **P1 (高)** | 数据同步`recordsSynced`始终为0 | `amazonSyncService` | 无法监控同步增量，数据健康度误报 | **待修复** |
| **P2 (中)** | 89%的竞价调整没有算法来源标签 | `bidOptimizer` | 算法效果追踪困难，无法评估各算法贡献 | **待修复** |
| **P2 (中)** | 前端多个页面数据显示不一致或为空 | `前端` | 用户体验差，无法获取真实系统状态 | **待修复** |
| **P2 (中)** | 确认同步(confirmationSync)从未触发 | `dataSyncScheduler` | 无法验证优化指令是否在Amazon端生效 | **待修复** |
| **P2 (中)** | 数据库查询存在全表扫描 (getPerformanceGroups) | `db/queries` | 性能问题，随数据量增长会变慢 | **待修复** |
| **P3 (低)** | 店铺名称映射不一致 (Kool Classic vs LERUCCI) | `数据` | 前端显示混乱 | **待修复** |
| **P3 (低)** | 4个账户无任何广告数据 | `数据同步` | 无法为这些账户提供服务 | **待修复** |

## 3. 优化方案

### 3.1 P0: 紧急修复 (立即执行)

1.  **修复API限流accountId=0问题**
    - **方案**: 审查`apiRateLimitService`调用链路，确保在所有API调用（特别是report端点）时都正确传递`accountId`。
    - **负责人**: Manus AI

2.  **修复RL奖励回填**
    - **方案**: 深入排查`backfillRewards`的触发条件和执行逻辑。检查`nextGenMaintenance`任务是否按预期执行，并修复导致`totalRLLogs=0`的根本原因。
    - **负责人**: Manus AI

3.  **升级实例配置**
    - **方案**: 将EB环境实例类型从`t3.small`升级到`t4g.medium`或`m5.large`，并将`--max-old-space-size`调整为实例内存的70%。同时，将ASG的MinSize提高到2，以保证高可用性。
    - **负责人**: Manus AI

### 3.2 P1: 高优先级修复

1.  **完善多租户隔离**
    - **方案**: 重新实现`tenantMiddleware`，从数据库中获取真实的租户信息、用户角色和API使用情况。实现真正的配额检查和功能开关。
    - **负责人**: Manus AI

2.  **解决优化锁冲突**
    - **方案**: 审查`lockManager`的实现，特别是锁的获取和释放逻辑。考虑使用更精细的锁粒度（例如，`accountId:module:entityId`），并为锁设置合理的超时时间。
    - **负责人**: Manus AI

3.  **修复同步记录数统计**
    - **方案**: 修改`amazonSyncService`，在`upsert`操作后，正确统计新增和更新的记录数，并更新到`sync_logs`表中。
    - **负责人**: Manus AI

### 3.3 P2/P3: 中低优先级修复

- **算法标签**: 为所有竞价调整添加来源算法标签。
- **前端修复**: 逐一修复前端数据显示问题，包括同步健康度、操作日志、算法效果等。
- **确认同步**: 在`dataSyncScheduler`中启用并验证确认同步任务。
- **数据库优化**: 为`getPerformanceGroupsByAccountId`等查询添加`WHERE`条件，避免全表扫描。
- **数据清洗**: 统一店铺名称，并排查4个无数据账户的原因。

## 4. 执行计划

我将立即开始按照以上优化方案，从P0紧急修复任务开始，逐一实施优化。完成后，将构建v369版本并部署到生产环境，然后开始新一轮的评估验证。
