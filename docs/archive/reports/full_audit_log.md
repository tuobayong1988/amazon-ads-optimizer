
# v399-fix2 生产环境审计笔记

## 部署状态确认
- **版本标签**: v399-fix2-20260310134946
- **部署ID**: 61
- **环境状态**: Ready / Green / Ok
- **实例类型**: t3.medium (2 vCPU, 4GB RAM)
- **实例数量**: 2个实例运行中
  - i-0688cea0ba48fcd63 (us-east-1c) - 启动于 2026-03-10T23:21:16Z
  - i-08456682570c1f813 (us-east-1b) - 启动于 2026-03-10T14:35:20Z
- **CPU使用率**: ~2.5% / ~1.8% (非常低)
- **负载**: 0.09 / 0.06 (非常低)
- **请求延迟**: P99 = 3ms (优秀)

## 注意事项
- EB事件日志显示曾出现vCPU限制(8 vCPU上限)，自动扩展从3到4实例失败
- 有实例频繁添加/移除的记录，可能是自动扩展策略过于敏感
- SYSTEM_VERSION 常量仍为 397，未更新到 399

## 代码版本
- HEAD: c54d7a6d - v399: fix duplicate accounts declarations
- package.json version: 3.9.9
# v399-fix2 前端审计笔记

## Dashboard (数据概览) 页面
- 已自动登录为系统管理员 vip@mucers.com
- 运营指挥中心显示日期范围: 2026-03-03 ~ 2026-03-10
- 最后同步: 03/10 16:31 PST
- 左侧导航菜单: 数据概览、优化中心、策略管理、广告活动、A/B测试、系统设置、Amazon API、优化设置、通知设置、团队与安全、系统监控、预发布引擎
- KPI卡片: 总花费、总销售额、平均ACoS、平均ROAS、TACoS、老板关注、总订单 - 数据加载中
- 同步健康度: 已同步0, 待同步0, 同步失败0
- 算法效果概览: 30天优化操作0次, 平均正向率0.0%, 最优算法无数据
- 系统健康监控: 暂无健康监控数据
- 账户风险排行: 健康0, 警告0, 严重0 - 暂无账户数据
- 订单趋势和花费vs销售额vs ACoS趋势图表存在

## 数据加载后实际状态
- 当前选择账户: ElaraFit, 站点: 美国
- KPI卡片: 总花费$9347(-21.0%), 总销售额$18890(-7.6%), 平均ACoS 49.5%(+6.0%), 平均ROAS 2.02(-6.0%), TACoS 33.0%(+4.2%), 总订单634(-7.6%)
- 同步健康度: 已同步169,274, 待同步172, 同步失败376, 不适用43,287, 同步成功率99.7% - 标记"需要关注"
- 算法效果概览: 30天优化操作5,000次, 平均正向率76.6%, 最优算法dayparting_engine
  - dayparting_engine 100%, budget_allocator 13%, keyword_status 100%, Cascade融合 59%, CQL强化学习 25%, 规则引擎 86%, LinUCB赌博机 43%
- 智能运营推荐: 正在扫描广告活动健康度
- 系统健康监控: 暂无健康监控数据
- 账户风险排行: 健康4, 警告0, 严重2
  - #1 Kool Classic US: ACoS 153.1%, ROAS 0.65, 花费$859, 销售$561, 17单
  - #2 US: ACoS 55.0%, ROAS 1.82, 花费$6339, 销售$11533, 409单 - 紧急优化中
  - #3 CA: ACoS 31.6%, ROAS 3.16, 花费$2149, 销售$6796, 208单
  - #4-6 MX/CA/MX: ACoS 0.0%, 无数据

## 初步问题记录
1. 系统健康监控无数据 - 需要排查
2. 同步失败376条 - 需要排查原因
3. budget_allocator正向率仅13% - 预算分配算法效果差
4. CQL强化学习正向率仅25% - 效果差
5. LinUCB赌博机正向率仅43% - 效果差
6. 3/9日期在订单趋势图中缺失 - 数据缺失
7. MX/CA站点无数据(花费$0, 销售$0) - 需确认是否正常

## 广告活动页面 (Campaigns)
- 总计2936个广告活动: SP自动140, SP手动1340, SB品牌1427, SD展示29
- 运行状态: 活跃302, 暂停2528
- 优化状态: 已介入2628, 未介入308
- 列设置包含: 广告活动、国家、状态、类型、开始日期、日预算、计费类型、广告目标、曝光、点击、CTR、当日花费、累计花费、当日销售额、累计销售额、ACoS、ROAS、优化目标、建议策略模板
- 注意: 大量广告活动近7天数据全部为0(花费$0, 销售$0) - 需要排查是否数据同步问题
- 部分广告活动已分配优化目标(如"普拉提球"策略aggressive-growth)
- 大量SB品牌广告活动数据为0 - 需要确认SB数据同步是否正常

## 策略管理页面 (Strategy Center)
- 优化目标: 0/0, 管理的广告活动: 0, 自动优化: 未启用, 优化模块: 7个
- "我的优化目标"显示"加载优化目标..."
- 策略模板库: 激进增长(ACoS 40%), 平衡增长(ACoS 25%), 利润优先(ACoS 15%), 旺季冲刺(ACoS 35%), 品牌防御(ACoS 10%)
- 问题: 优化目标显示0/0但广告活动页面显示有已分配优化目标的活动 - 数据不一致

## Amazon API 多账号管理页面
- 总店铺数: 2 (ElaraFit, Kool Classic)
- 已连接: 6
- 待配置: 0
- 连接错误: 0
- 市场覆盖: 3 (加拿大、墨西哥、美国)
- ElaraFit: 3个站点(加拿大、墨西哥、美国) - 全部已连接
- Kool Classic: 3个站点(加拿大、墨西哥、美国) - 全部已连接

### 数据同步标签
- 店铺数据同步 - ElaraFit
- 同步站点: 加拿大、墨西哥、美国
- 增量同步开关已启用
- 功能按钮: 定时同步、队列、冲突0、查看历史、增量同步、运行自动优化

## 系统健康监控 - 系统资源
- CPU使用率: 5.4%, 2核心 Intel Xeon Platinum 8175M @ 2.50GHz, 负载: 0.22(1m)/0.12(5m)/0.09(15m)
- 系统内存: 31.6%, 已用1215MB/总计3840MB, 可用2625MB
- Node.js进程内存: 4.8%, RSS 483.1MB, 堆已用149.2MB, 堆总计208.2MB, 外部3.6MB, V8堆上限3120MB(已用149.2MB/3120MB)
- 数据库连接池: 连接池正常, 配置上限60, 已创建1502, 健康检查失败0, 连接池重建0, 借出连接695, 归还连接694, 疑似泄漏1个连接
- 系统信息: linux/x64, Node.js v20.20.0, 运行时间5.8小时, 数据更新11:36:22 PM

### 关键问题
- 疑似泄漏1个连接 - 借出695 vs 归还694
- 已创建1502个连接(上限60) - 说明连接频繁重建
- 健康概览标签页数据加载中但无法显示(骨架屏状态)

## 纠错监控页面 (correction-review) - 严重BUG
- **页面崩溃**: ReferenceError: Cannot access 'W' before initialization
- 错误来源: CorrectionReview-DR84xUmb.js:1:2239
- 这是一个前端代码分割/打包问题，变量在初始化前被访问

## 同步日志页面 (sync-logs)
- 总计622条记录
- 成功19, 警告0, 错误1
- 最新错误(#4109, 2026-03-10 19:32:21): SP广告位绩效查询失败 - SQL查询campaigns表字段过多，账户90023同步超时(60分钟>阈值60分钟)，已完成23/31步骤
- 多次同步超时记录:
  - #3987: 账户90023同步超时(45分钟>阈值45分钟)，完成21/30步骤
  - #3967: 账户90023同步超时(52分钟>阈值45分钟)，完成13/30步骤
  - #3949: 账户90023同步超时(52分钟>阈值45分钟)，完成13/30步骤
- 关键问题: 账户90023(ElaraFit US)的同步频繁超时，campaigns表查询字段过多导致性能问题

## 数据健康仪表盘
- 综合健康评分: 100 (健康), 最后检查: 3/10/2026 11:39:38 PM
- 数据同步: 95%, 24h成功率(38/40)
- API限流: 运行中, 21个端点监控中
- 自愈调度器: 运行中, 已执行89次自愈任务
- 指令确认: 运行中, 待确认2条
- 最近同步任务(全部完成):
  - 账户#90026: 9/9步骤完成(多次)
  - 账户#90021: 9/9步骤完成
  - 账户#90022: 9/9步骤完成
  - 账户#90025: 31/31步骤完成
  - 账户#90026: 31/31步骤完成
- 注意: 9/9步骤 vs 31/31步骤 - 不同账户同步步骤数差异很大，可能是增量vs全量同步

## 优化设置页面
- 账号设置、优化参数、日内竞价、组织管理、引导设置
- 优化参数: 默认最高出价$2.00, 转换类型为销售额, 转换数据来源为平台数据
- 出价调整限制: 单次调整上限25%, 最低出价$0.02, 数据要求5次点击

## 自动优化仪表盘 (auto-optimization-dashboard)
- 页面一直显示"加载优化仪表板中..."，无法加载完成 - 可能是后端API超时或数据量过大

## 数据校验页面 (data-validation)
- 功能: 对比本地数据与亚马逊后台数据，自动发现差异
- 本地数据统计: 显示"加载中..."
- 准备就绪状态，可以点击"开始校验"

## 优化目标页面 (optimization-targets)
- 优化目标总数: 5, 已介入广告活动: 2628, 未介入: 308, 优化中/已暂停: 4/1

| 优化目标 | 广告活动数 | 状态 | 费用上限 | 达成度 | 花费 | 销售 | 订单 | ACoS | ROAS | CTR | CVR |
|---------|---------|------|---------|--------|------|------|------|------|------|-----|-----|
| 普拉提球 | 68 | 优化中 | $200 | 一般45分 | $2865 | $2547 | 98 | 112.5% | 0.89 | 0.29% | 6.21% |
| 普拉提套装产品优化 | 40 | 优化中 | $500 | 待改善33分 | $10550 | $19422 | 588 | 54.3% | 1.84 | 2.21% | 5.98% |
| 普拉提平衡板产品优化 | 10 | 已暂停 | $200 | 一般61分 | $9659 | $27351 | 935 | 35.3% | 2.83 | 1.45% | 20.79% |
| 普拉提足底板 | 28 | 优化中 | $200 | 一般56分 | $872 | $1582 | 74 | 55.1% | 1.81 | 0.75% | 15.55% |
| 普拉提袜产品优化 | 45 | 优化中 | $200 | 待改善22分 | $2002 | $1325 | 77 | 151.1% | 0.66 | 0.25% | 7.96% |

### 关键问题
- 5个优化目标管理68+40+10+28+45=191个广告活动，但已介入显示2628 - 数据不一致
- 普拉提球ACoS 112.5%极高，ROAS仅0.89 - 优化效果差
- 普拉提袜ACoS 151.1%极高，ROAS仅0.66 - 优化效果差
- 目标达成度评分普遍偏低(22-61分)


## 前端代码深度审计

### 路由和页面完整性

| 页面 | 路由 | 状态 | 问题 |
|------|------|------|------|
| Dashboard | / | ✅ 正常 | 数据展示正常 |
| 策略中心 | /strategy-center | ⚠️ 数据不一致 | 优化目标显示0/0 |
| 广告活动 | /campaigns | ✅ 正常 | 数据加载正常 |
| 优化目标列表 | /optimization-targets | ✅ 正常 | 目标达成度进度条正常 |
| Amazon API | /amazon-api | ✅ 正常 | 同步状态展示正常 |
| 系统健康 | /system-health | ✅ 正常 | 监控数据正常 |
| 数据健康 | /data-health | ✅ 正常 | 数据质量展示正常 |
| 同步日志 | /sync-logs | ✅ 正常 | 日志展示正常 |
| 纠错监控 | /auto-correction | ✅ 后端路由存在 | 需验证前端是否正常 |
| 自动优化仪表盘 | /auto-optimization-dashboard | ❌ 加载失败 | 一直显示加载中 |
| 纠错审核 | /correction-review | ❌ 崩溃 | ReferenceError |
| 数据验证 | /data-validation | ✅ 正常 | 验证功能正常 |
| 优化设置 | /settings | ✅ 正常 | 设置项完整 |

### 前端架构评估

1. **代码分割**：70个页面组件使用React.lazy()懒加载，LazyRoute包装 ✅
2. **全局账户选择器**：支持多店铺多站点切换，localStorage持久化 ✅
3. **ErrorBoundary**：全局错误边界存在，但错误信息为英文 ⚠️
4. **目标达成度进度条**：基于七维度加权评分算法，前端展示完整 ✅
5. **多租户数据隔离**：后端API路由使用ctx.user.id过滤 ✅

### 关键前端问题

1. **AutoOptimizationDashboard加载问题**：后端路由存在，v378已修复trpc调用，可能是数据查询慢
2. **CorrectionReview页面崩溃**：ReferenceError - 代码打包/变量初始化问题
3. **Dashboard与OptimizationTargets目标达成度计算不一致**：Dashboard用简单比值，OptimizationTargets用七维度评分
4. **策略中心优化目标显示0/0**：可能是store/marketplace过滤问题
# v399-fix2 数据同步模块审计笔记

## 1. 同步架构概述

系统采用DAG并行调度的5层同步架构（v359/v360），按层级依赖关系并行执行：

| 层级 | 同步内容 | 并行数 | 层间延迟 |
|------|---------|--------|---------|
| Layer 0 | SP/SB/SD广告活动 | 3 | 2000ms |
| Layer 1 | SP/SB/SD广告组 | 3 | 2000ms |
| Layer 2 | 关键词+商品定位+素材 | 6 | 2000ms |
| Layer 3 | 否定词+搜索词+广告位绩效 | 9 | 2000ms |
| Layer 4 | 定向报告+素材URL | 4 | 2000ms |
| Layer 5 | 绩效数据(SP/SB/SD+关键词+商品定位+广告组) | 4 | - |

## 2. 三阶段同步策略（v382）

| 模式 | SP天数 | SB天数 | SD天数 | 使用场景 |
|------|--------|--------|--------|---------|
| init | 90 | 60 | 90 | 新账号初始化 |
| daily | 14 | 14 | 14 | 日常增量同步 |
| recovery | 90 | 60 | 90 | 自愈恢复 |

## 3. 覆盖式回填实现分析

### 3.1 daily_performance表 - 正确实现覆盖式
使用 `onDuplicateKeyUpdate` 基于唯一约束 `uk_daily_perf (accountId, campaignId, date, adType)` 实现UPSERT。同一日期+同一广告活动+同一广告类型的数据会被覆盖而非累加。**这是正确的实现方式。**

### 3.2 搜索词表 - 正确实现覆盖式
使用 `flushSearchTermBatch` 函数，通过 `onDuplicateKeyUpdate` 实现UPSERT。同一搜索词的数据会被覆盖。

### 3.3 自动定向数据 - 存在问题
`syncAutoTargeting` 方法使用逐条 `existing` 检查 + `update/insert` 模式，而非批量UPSERT。这种方式：
- 性能差：每条数据需要2次数据库查询（查adGroup + 查existing）
- 存在N+1查询问题
- 但覆盖逻辑本身是正确的（有则更新，无则插入）

## 4. 关键问题发现

### P0 - 严重问题

1. **自动定向同步N+1查询问题**：`syncAutoTargeting` 中对每条数据都执行 `db.select().from(adGroups)` 和 `db.select().from(productTargets)` 查询，在大数据量下会严重拖慢同步速度。

2. **搜索词campaignId使用不一致**：搜索词数据中 `campaignId` 字段存储的是 `campaign.campaignId`（Amazon的campaignId），而不是本地数据库的 `campaign.id`。这可能导致关联查询时出现问题。

### P1 - 重要问题

3. **出价/预算保护机制降级风险**：`getRecentlyOptimizedKeywordIds` 在查询失败时返回空Set，这意味着保护机制完全失效，可能导致优化后的出价被API数据覆盖。

4. **syncAllAdData 方法是串行执行**：与 `syncAll` 的DAG并行不同，`syncAllAdData` 完全串行执行所有步骤，效率低下。

5. **数据保留期clamp使用UTC时间**：`clampStartDateForRetention` 使用 `new Date()` 计算安全日期，但没有考虑站点时区。

### P2 - 一般问题

6. **大量@ts-ignore注释**：代码中有大量 `@ts-ignore` 注释，表明类型系统不够严谨。

7. **步骤级重试配置硬编码**：重试次数、延迟等参数硬编码在代码中，无法通过环境变量调整。

## 5. 数据覆盖式回填总结

| 数据类型 | 覆盖方式 | 是否正确 | 备注 |
|---------|---------|---------|------|
| daily_performance | onDuplicateKeyUpdate | 是 | 基于唯一约束覆盖 |
| 搜索词 | onDuplicateKeyUpdate | 是 | 批量UPSERT |
| 自动定向 | 逐条check+update/insert | 是(但低效) | N+1查询问题 |
| 关键词绩效 | 待确认 | 待确认 | 需要检查syncKeywordPerformanceData |
| 商品定位绩效 | 待确认 | 待确认 | 需要检查syncProductTargetPerformanceData |


## 6. 关键词绩效同步 - 重大问题发现

### P0 - 严重数据累加问题
`syncKeywordPerformanceData` 使用SUMMARY模式报告，在分批请求时（每批31天），同一keyword在不同批次中会出现。虽然v395添加了聚合逻辑（按targetId/keywordId累加），但这个聚合本身就是**累加**而非覆盖。

关键问题：关键词/商品定位的绩效数据直接写入 `keywords` 和 `product_targets` 表的字段中（如impressions, clicks, spend, sales等），**每次同步都会用累加后的数据覆盖这些字段**。这意味着：
- 如果同步90天数据，keywords表中的spend/sales等字段存储的是90天的累计值
- 如果同步14天数据，存储的是14天的累计值
- **不同同步天数会导致不同的绩效数据，这是不精确的**

### P1 - 关键词绩效不是按日存储
与 `daily_performance` 表不同，关键词绩效数据直接更新到 `keywords` 表的汇总字段中，没有按日期维度存储。这导致：
- 无法查看关键词的历史趋势
- 无法精确对比不同时间段的关键词表现
- 同步天数变化会导致数据不一致

### P2 - 小时级数据是模拟生成的
`generateHourlyFromDaily` 使用固定的流量分布模型从daily数据生成hourly数据，这不是真实的小时级数据。对于"分时竞价"功能来说，这个模拟数据的精度可能不够。

## 7. 数据同步完整性评估

| 广告类型 | 广告活动 | 广告组 | 关键词 | 商品定位 | 否定词 | 搜索词 | 绩效数据 | 广告位绩效 |
|---------|---------|--------|--------|---------|--------|--------|---------|----------|
| SP | 有 | 有 | 有 | 有 | 有(关键词+商品) | 有 | 有 | 有 |
| SB | 有 | 有 | 有 | 有 | 有(关键词+商品) | 有 | 有 | 有 |
| SD | 有 | 有 | N/A | 有 | 有(v382新增) | N/A | 有 | N/A |

SD搜索词和SD广告位绩效缺失，但SD广告类型本身不支持搜索词报告，这是正常的。
# v399-fix2 智能优化模块审计笔记

## 1. 优化目标引擎架构

系统采用 `optimizationTargetEngine.ts` 作为统一入口，通过子模块执行器实现各优化策略：

| 模块 | 执行器文件 | 功能 |
|------|-----------|------|
| 出价优化 | bidOptimizationExecutor.ts | 基于绩效数据调整关键词/商品定位出价 |
| 位置优化 | placementExecutor.ts | 调整广告活动位置百分比（首页/商品页） |
| 分时竞价 | daypartingExecutor.ts | 基于小时级规则调整关键词出价 |
| 分时预算 | daypartingExecutor.ts | 基于星期几调整广告活动预算 |
| 搜索词分析 | searchTermExecutor.ts | 智能否词、收割搜索词、ASIN定向 |
| 预算分配 | budgetExecutor.ts | 组合级预算分配优化 |
| 状态变更 | statusChangeExecutor.ts | 关键词/广告组/广告活动暂停/启用 |
| 竞价协调 | bidCoordinationExecutor.ts | 中央竞价协调，统一处理各服务建议 |

## 2. 优化模块完整性评估

### 已实现的优化功能
- [x] 分时竞价（基于小时级数据，多维度组合分析）
- [x] 分时预算（基于星期几调整预算）
- [x] 位置倾斜（首页/商品页百分比调整）
- [x] 智能否词（关键词否定 + 产品否定）
- [x] 智能否ASIN（CREATE_NEGATIVE_PRODUCT_TARGET）
- [x] 搜索词收割（CREATE_KEYWORD + ASIN重定向到product target）
- [x] Ngram自动否定分析
- [x] 品牌词保护
- [x] 探索期保护
- [x] 生命周期感知调度
- [x] 自我进化引擎
- [x] 安全护栏（出价/预算/位置）
- [x] 紧急优化模式
- [x] 失败任务重试队列

### 优化目标达成度进度条
需要在前端确认是否有对应的展示组件。

## 3. 关键问题发现

### P0 - 严重问题

1. **分时竞价基于模拟数据**：`generateHourlyFromDaily` 使用固定的流量分布模型从daily数据生成hourly数据。分时竞价的规则计算 `analyzeHourlyPerformance` 依赖的是这些模拟的小时级数据，而非真实的小时级数据。这意味着分时竞价的精度受限于模拟数据的准确性。

2. **分时竞价修改baseBid但不恢复**：分时竞价将关键词的出价从baseBid调整为adjustedBid（baseBid × multiplier），但代码中没有看到在时段结束后将出价恢复到baseBid的逻辑。这意味着如果某个时段将出价提高了40%，在下一个时段如果multiplier是1.0，则出价不会变回原来的baseBid。

3. **搜索词绩效数据是累计值**：搜索词的clicks/impressions/orders/spend/sales是SUMMARY模式下的累计值（跨多个批次聚合），而非按日期维度的数据。这可能导致否词/收割决策基于不精确的数据。

### P1 - 重要问题

4. **分时竞价对所有关键词逐条API调用**：每个campaign的每个enabled关键词都会单独调用 `syncBidAdjustmentsToAmazon`，在大量关键词的情况下会产生大量API调用，容易触发限流。

5. **搜索词分析对每个campaign逐条查询DB**：`getKeywordsByCampaignId`、`getAdGroupsByCampaignId`、`getSearchTermsByCampaignId` 在循环内被调用，存在N+1查询问题。

6. **否定关键词去重查询在循环内**：每个否定关键词决策都会查询DB检查是否已存在，应该预加载。

## 4. API限流服务评估

### 架构
- 使用令牌桶算法 + 滑动窗口计数器
- 分端点类型（list/mutate/report/snapshot）
- Per-account退避 + 全局TPS上限
- 内存存储（单实例部署）

### 限流配置

| 端点类型 | Per-Account TPS | 全局TPS | Per-Account/分钟 | 全局/分钟 |
|---------|----------------|---------|-----------------|----------|
| list | 8 | 30 | 400 | 1500 |
| mutate | 4 | 15 | 200 | 750 |
| report | 1 | 5 | 30 | 150 |
| snapshot | 1 | 3 | 20 | 60 |

### 问题
1. **内存存储不支持分布式**：如果未来扩展到多实例，限流状态无法共享。
2. **全局TPS上限可能不够**：200-500租户场景下，全局30 TPS的list端点可能不够用。
3. **退避恢复可能过快**：指数恢复策略在高并发下可能导致频繁触发429。

## 5. 多租户数据隔离评估

### 已实现的隔离措施
- v387: 关键词绩效同步添加accountId过滤
- v387: 商品定位绩效同步添加accountId过滤
- v387: 广告组预加载添加accountId过滤
- 搜索词分析中关键词去重检查添加accountId过滤
- 优化目标引擎通过accountId获取campaigns

### 潜在隔离风险
1. **关键词文本匹配兜底策略**：`kwByText` Map按纯文本匹配，如果两个不同账户有相同的关键词文本，可能会匹配到错误的关键词。但由于预加载时已按accountId过滤，这个风险已被消除。
2. **否定关键词去重**：使用campaignId过滤，campaignId在不同账户间是唯一的（Amazon分配），隔离正确。


## 6. 多租户数据隔离详细评估

### 数据同步层隔离

| 模块 | accountId过滤 | 隔离状态 | 备注 |
|------|-------------|---------|------|
| Campaign同步 | ✅ | 正确 | eq(campaigns.accountId, this.accountId) |
| AdGroup同步 | ✅ | 正确 | eq(adGroups.accountId, this.accountId) |
| Keyword同步 | ✅ | 正确 | eq(keywords.accountId, this.accountId) |
| ProductTarget同步 | ✅ | 正确 | eq(productTargets.accountId, this.accountId) |
| SearchTerm同步 | ✅ | 正确 | eq(searchTerms.accountId, this.accountId) |
| 绩效数据预加载 | ✅ v387修复 | 正确 | 关键词/商品定位/广告组均按accountId过滤 |
| DailyPerformance UPSERT | ✅ | 正确 | 唯一约束包含accountId |

### 优化层隔离

| 模块 | 隔离机制 | 状态 |
|------|---------|------|
| 搜索词分析 | performanceGroupId + accountId | ✅ 正确 |
| 分时竞价 | config.accountId → campaigns过滤 | ✅ 正确 |
| 出价优化 | config.accountId | ✅ 正确 |
| 否定关键词去重 | campaignId (Amazon唯一) | ✅ 正确 |
| 锁管理 | accountId + module组合锁 | ✅ 正确 |

### 潜在风险点

1. **kwByText纯文本兜底匹配**：虽然预加载时已按accountId过滤，但如果同一账户内有重复的keywordText，最后一个会覆盖前一个。这不是跨租户问题，但可能导致同一账户内的数据匹配不准确。

2. **分布式锁连接泄漏**：lockManager使用MySQL GET_LOCK，每个锁持有一个连接。在500租户场景下，如果大量账户同时获取锁，可能耗尽连接池。当前连接池大小25，分布式锁清理间隔5分钟，最大持有30分钟。

## 7. 同步调度架构评估（200-500租户）

### 当前配置

| 参数 | 值 | 说明 |
|------|---|------|
| 高频同步间隔 | 15分钟 | 广告活动状态、预算 |
| 中频同步间隔 | 30分钟 | 广告组、关键词、定位 |
| 完整同步间隔 | 2小时 | 所有数据 |
| 高频最大账号数 | 80 | 每周期 |
| 中频最大账号数 | 120 | 每周期 |
| 完整同步最大账号数 | 200 | 每周期 |
| 最大并发账号数 | 15 | 可通过环境变量调整 |
| 连接池大小 | 25 | 可通过环境变量调整 |
| 连接泄露超时 | 120秒 | 自动回收 |

### 500租户场景分析

- 完整同步：每2小时最多200个账号，500个账号需要3个周期（6小时）完成一轮
- 高频同步：每15分钟最多80个账号，500个账号需要7个周期（约2小时）
- 中频同步：每30分钟最多120个账号，500个账号需要5个周期（约2.5小时）

### 关键瓶颈

1. **数据库连接池**：25个连接，15个并发账号 + 分布式锁连接 + 健康检查 = 可能不够
2. **API全局TPS**：list 30 TPS，500个账号每个需要多次API调用，可能导致频繁限流
3. **内存限流存储**：单实例部署，无法水平扩展
4. **单实例瓶颈**：所有同步和优化都在一个Node.js进程中运行
# v399-fix2 系统承载能力评估

## 当前基础设施配置

| 组件 | 规格 | 资源 |
|------|------|------|
| EC2实例 | t3.small | 2 vCPU, 2GB RAM |
| RDS数据库 | db.t4g.small | 2 vCPU, 2GB RAM, 20GB存储 |
| 实例数量 | 1 (MinSize=1, MaxSize=1) | 单实例部署 |
| 负载均衡 | ALB | LoadBalanced环境 |
| Node.js内存 | --max-old-space-size=2048 | 2048MB (等于物理内存) |

## 关键环境变量

| 参数 | 值 | 说明 |
|------|---|------|
| DB_POOL_SIZE | 60 | 数据库连接池大小 |
| MAX_CONCURRENT_ACCOUNTS | 50 | 最大并发同步账号数 |
| NODE_OPTIONS | --max-old-space-size=2048 | Node.js堆内存上限 |
| GRACEFUL_SHUTDOWN_TIMEOUT | 25 | 优雅关闭超时秒数 |

## 500租户场景分析

### 数据同步吞吐量

| 同步层级 | 间隔 | 每周期最大账号 | 500账号需要周期数 | 完成一轮时间 |
|---------|------|-------------|----------------|-----------|
| 高频 | 15分钟 | 80 | 7 | ~2小时 |
| 中频 | 30分钟 | 120 | 5 | ~2.5小时 |
| 完整 | 2小时 | 200 | 3 | ~6小时 |

### 关键瓶颈分析

#### 1. 内存瓶颈 (严重)
- t3.small只有2GB物理内存
- Node.js设置max-old-space-size=2048MB，等于全部物理内存
- 操作系统本身需要约300-500MB
- 当前实际使用: RSS 483MB, 堆149MB
- 500租户场景下，内存Map预加载数据会显著增加
- 风险: OOM Kill导致进程崩溃

#### 2. 数据库瓶颈 (严重)
- db.t4g.small只有2GB RAM，MySQL max_connections约150
- 应用连接池60 + 分布式锁连接 + 其他连接 > 100
- 20GB存储对500租户远远不够
- 500租户 × 90天 × 多广告类型 × 多层级 = 海量数据
- 风险: 存储空间耗尽，连接数耗尽

#### 3. CPU瓶颈 (中等)
- 2 vCPU，Node.js单线程
- 50并发账号同步 + 优化计算 + API请求
- 当前CPU使用率5.4%（仅6个账号）
- 500租户预计: 5.4% × (500/6) ≈ 450% (远超2核能力)

#### 4. API限流瓶颈 (中等)
- Amazon API全局TPS: list 30, mutate 10
- 50并发账号各发API请求，实际TPS可能超限
- 动态降级机制存在，但会显著降低同步速度
- 500租户场景下，API限流会成为主要瓶颈

#### 5. 单实例瓶颈 (严重)
- MinSize=1, MaxSize=1，无法水平扩展
- 所有同步、优化、API请求都在一个进程中
- 进程崩溃 = 全部服务中断
- 无法利用多实例分担负载

### 容量估算

| 指标 | 当前(6账号) | 200租户 | 500租户 | 是否可行 |
|------|-----------|---------|---------|---------|
| 内存使用 | 483MB | ~2GB | ~5GB | 200: 勉强, 500: 不可行 |
| CPU使用 | 5.4% | ~180% | ~450% | 200: 不可行, 500: 不可行 |
| DB连接 | ~10 | ~60 | ~100+ | 200: 可行, 500: 勉强 |
| DB存储 | ~2GB | ~30GB | ~80GB | 200: 不可行(20GB), 500: 不可行 |
| API TPS | ~5 | ~100 | ~250 | 200: 受限, 500: 严重受限 |
| 同步周期 | ~5分钟 | ~2小时 | ~6小时 | 200: 可接受, 500: 勉强 |

## 建议升级方案

### 短期 (200租户)
- EC2: t3.small → t3.medium (2 vCPU, 4GB RAM)
- RDS: db.t4g.small → db.t4g.medium (2 vCPU, 4GB RAM)
- RDS存储: 20GB → 100GB
- Node.js: --max-old-space-size=3072

### 中期 (500租户)
- EC2: t3.medium → t3.large (2 vCPU, 8GB RAM) 或 c5.large (2 vCPU, 4GB RAM, 高CPU)
- RDS: db.t4g.medium → db.r6g.large (2 vCPU, 16GB RAM)
- RDS存储: 100GB → 500GB
- 实例数量: 1 → 2-3 (需要实现无状态化)
- 引入Redis缓存层
