# Amazon Ads Optimizer 算法自主决策引擎架构设计

## 一、设计理念

### 核心转变

传统的广告优化工具采用"人工操作为主、工具辅助"的模式，运营人员需要手动分析数据、制定策略、执行操作。这种模式存在以下问题：操作效率低、响应速度慢、依赖个人经验、难以24小时持续优化。

本系统采用全新的设计理念：**算法自主决策执行、人工仅做监督调整**。系统将自动完成数据分析、策略制定、操作执行的全流程，运营人员的角色从"操作者"转变为"监督者"，主要负责设定目标、审核异常、调整参数。

### 设计原则

| 原则 | 说明 |
|------|------|
| 自主性 | 系统能够独立完成从数据采集到执行优化的全流程 |
| 透明性 | 所有决策逻辑和执行记录对用户可见可追溯 |
| 可控性 | 用户可随时干预、暂停、回滚任何自动化操作 |
| 学习性 | 系统能够从历史数据中学习，持续优化决策质量 |
| 安全性 | 设置合理的边界条件，防止异常操作造成损失 |

---

## 二、系统架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         用户界面层 (UI Layer)                            │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │ 监控仪表盘   │  │ 算法控制台   │  │ 执行日志    │  │ 效果复盘    │    │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘    │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      决策引擎层 (Decision Engine)                        │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                    算法自主决策引擎 (Core)                        │   │
│  │  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐    │   │
│  │  │ 数据分析   │  │ 规则引擎   │  │ 决策生成   │  │ 执行调度   │    │   │
│  │  │ Analyzer  │→ │ Rules     │→ │ Decision  │→ │ Scheduler │    │   │
│  │  └───────────┘  └───────────┘  └───────────┘  └───────────┘    │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │ 竞价优化器   │  │ 位置倾斜器   │  │ 分时策略器   │  │ 否词分析器   │   │
│  │ BidOptimizer│  │ Placement  │  │ Dayparting │  │ Negative   │   │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                       执行层 (Execution Layer)                           │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │ API执行器   │  │ 批量处理器   │  │ 回滚管理器   │  │ 日志记录器   │   │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        数据层 (Data Layer)                               │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐   │
│  │ 实时数据    │  │ 历史数据    │  │ 执行记录    │  │ 学习模型    │   │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 三、核心模块设计

### 3.1 数据分析模块 (Analyzer)

数据分析模块负责从原始数据中提取有价值的洞察，为决策提供依据。

**输入数据源**：
- 广告活动表现数据（展示、点击、花费、销售额、订单）
- 投放词级别数据（关键词、商品定向）
- 搜索词报告数据
- 广告位置表现数据（Top of Search、Product Page、Rest of Search）
- 历史趋势数据

**分析维度**：

| 分析类型 | 说明 | 输出 |
|----------|------|------|
| 效率分析 | 计算ACoS、ROAS、CTR、CVR等效率指标 | 效率评分 |
| 趋势分析 | 识别指标的上升/下降趋势 | 趋势方向和幅度 |
| 异常检测 | 发现偏离正常范围的数据点 | 异常标记和严重程度 |
| 机会识别 | 发现潜在的优化机会 | 机会列表和优先级 |
| 风险评估 | 评估当前状态的风险程度 | 风险等级 |

**分析算法示例**：

```typescript
interface AnalysisResult {
  targetId: number;
  targetType: 'keyword' | 'productTarget';
  metrics: {
    acos: number;
    roas: number;
    ctr: number;
    cvr: number;
    cpc: number;
  };
  trends: {
    direction: 'up' | 'down' | 'stable';
    magnitude: number; // 变化幅度百分比
    confidence: number; // 置信度 0-1
  };
  anomalies: {
    type: string;
    severity: 'low' | 'medium' | 'high';
    description: string;
  }[];
  opportunities: {
    type: string;
    priority: number;
    expectedImpact: number;
  }[];
}
```

### 3.2 规则引擎 (Rules Engine)

规则引擎将分析结果转化为具体的优化建议，支持系统预设规则和用户自定义规则。

**规则类型**：

| 规则类别 | 示例规则 | 触发条件 |
|----------|----------|----------|
| 竞价规则 | 高ACoS降价 | ACoS > 目标ACoS × 1.2 且 花费 > $10 |
| 竞价规则 | 低ACoS加价 | ACoS < 目标ACoS × 0.8 且 订单 > 3 |
| 竞价规则 | 0转化降价 | 花费 > $15 且 订单 = 0 |
| 状态规则 | 暂停低效词 | 花费 > $30 且 订单 = 0 且 持续7天 |
| 否词规则 | 否定低效搜索词 | 搜索词花费 > $10 且 订单 = 0 |
| 迁移规则 | 高价值词迁移 | 搜索词订单 > 5 且 ACoS < 20% |
| 位置规则 | 调整位置倾斜 | 位置ROAS差异 > 30% |

**规则执行优先级**：

1. 安全规则（防止超支、防止暂停高转化词）
2. 紧急规则（处理异常情况）
3. 优化规则（提升效率）
4. 增长规则（扩大规模）

### 3.3 决策生成模块 (Decision Generator)

决策生成模块将规则引擎的输出转化为具体的执行计划。

**决策类型**：

```typescript
type DecisionType = 
  | 'bid_adjustment'      // 出价调整
  | 'status_change'       // 状态变更
  | 'negative_keyword'    // 添加否定词
  | 'keyword_migration'   // 关键词迁移
  | 'placement_adjustment' // 位置倾斜调整
  | 'budget_adjustment'   // 预算调整
  | 'dayparting_change';  // 分时策略调整

interface Decision {
  id: string;
  type: DecisionType;
  targetId: number;
  targetType: string;
  action: {
    field: string;
    currentValue: any;
    newValue: any;
  };
  reason: string;
  confidence: number;
  expectedImpact: {
    metric: string;
    change: number;
  };
  riskLevel: 'low' | 'medium' | 'high';
  requiresApproval: boolean;
}
```

**决策合并与冲突解决**：

当多个规则对同一目标产生不同决策时，系统按以下优先级处理：
1. 安全规则优先于优化规则
2. 高置信度决策优先于低置信度
3. 保守决策优先于激进决策
4. 最近用户干预的方向优先

### 3.4 执行调度模块 (Scheduler)

执行调度模块负责将决策按照合理的时机和顺序执行。

**调度策略**：

| 策略 | 说明 | 适用场景 |
|------|------|----------|
| 即时执行 | 决策生成后立即执行 | 紧急情况、低风险操作 |
| 批量执行 | 累积一定数量后批量执行 | 常规优化操作 |
| 定时执行 | 在指定时间执行 | 分时策略、预算调整 |
| 审批执行 | 等待用户审批后执行 | 高风险操作、大幅调整 |

**时段分析策略（参考Adspert）**：

系统将每天24小时分割为12个2小时时段进行数据分析和决策：

| 时段 | 时间范围 | 分析内容 |
|------|----------|----------|
| T1 | 00:00-02:00 | 凌晨低峰期表现 |
| T2 | 02:00-04:00 | 凌晨低峰期表现 |
| T3 | 04:00-06:00 | 早晨启动期表现 |
| T4 | 06:00-08:00 | 早高峰期表现 |
| T5 | 08:00-10:00 | 上午高峰期表现 |
| T6 | 10:00-12:00 | 上午高峰期表现 |
| T7 | 12:00-14:00 | 午间表现 |
| T8 | 14:00-16:00 | 下午高峰期表现 |
| T9 | 16:00-18:00 | 下午高峰期表现 |
| T10 | 18:00-20:00 | 晚间高峰期表现 |
| T11 | 20:00-22:00 | 晚间高峰期表现 |
| T12 | 22:00-24:00 | 夜间低峰期表现 |

每个时段独立计算以下指标：
- 转化率（CVR）
- 广告投入产出比（ACoS/ROAS）
- 点击率（CTR）
- 平均点击成本（CPC）

**执行频率控制**：

为避免频繁调整影响广告表现，系统设置以下限制：
- 同一投放词24小时内最多调整**1次**出价
- 同一广告活动24小时内最多调整**1次**预算
- 位置倾斜比例每周最多调整**2次**
- 批量操作每次最多影响**100个**投放词

**调整冷却期机制**：

系统记录每次调整的时间戳，在冷却期内不会重复调整同一目标：
- 投放词出价冷却期：24小时
- 广告活动预算冷却期：24小时
- 位置倾斜冷却期：3.5天（确保每周最多2次）
- 否定词操作无冷却期（但有每日数量限制）

---

## 四、自动化功能整合

### 4.1 整合前的功能分布

```
┌─────────────────────────────────────────────────────────────────┐
│                     智能优化（独立模块）                          │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────┐    │
│  │ 广告自动化 │  │ 分时策略   │  │ 批量操作   │  │ 纠错复盘   │    │
│  └───────────┘  └───────────┘  └───────────┘  └───────────┘    │
└─────────────────────────────────────────────────────────────────┘
                              ↓ 整合
┌─────────────────────────────────────────────────────────────────┐
│                     广告管理（整合后）                            │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              算法自主决策引擎（内嵌）                      │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │   │
│  │  │自动竞价  │ │分时调整  │ │自动否词  │ │效果复盘  │       │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘       │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 整合后的用户体验

**广告活动卡片（整合视图）**：

```
┌─────────────────────────────────────────────────────────────────┐
│ 📊 活动名称: Summer Sale Campaign                               │
│ 类型: SP手动 │ 状态: 运行中 │ 日预算: $100                       │
├─────────────────────────────────────────────────────────────────┤
│ 核心指标                                                        │
│ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐   │
│ │花费 $85 │ │销售$320 │ │ACoS 27% │ │ROAS 3.8 │ │订单 12  │   │
│ │↑5%     │ │↑12%    │ │↓2%     │ │↑8%     │ │↑3      │   │
│ └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘   │
├─────────────────────────────────────────────────────────────────┤
│ 🤖 算法状态: 运行中                                             │
│ ┌─────────────────────────────────────────────────────────┐   │
│ │ ✅ 自动竞价: 已启用 │ 今日调整 5 个投放词                  │   │
│ │ ✅ 分时策略: 已启用 │ 当前时段出价 +15%                   │   │
│ │ ✅ 位置倾斜: 已启用 │ Top: +40% │ PP: +10% │ Rest: 0%    │   │
│ │ ✅ 自动否词: 已启用 │ 本周否定 3 个搜索词                  │   │
│ └─────────────────────────────────────────────────────────┘   │
│ [查看详情] [调整参数] [暂停算法] [查看日志]                      │
├─────────────────────────────────────────────────────────────────┤
│ ⚡ 待处理建议 (2)                                               │
│ • 发现1个高花费0转化投放词，建议暂停 [执行] [忽略]              │
│ • 发现1个高价值搜索词，建议迁移到精准匹配 [执行] [忽略]         │
└─────────────────────────────────────────────────────────────────┘
```

### 4.3 算法控制面板

用户可以在每个广告活动中控制算法的行为：

```
┌─────────────────────────────────────────────────────────────────┐
│ ⚙️ 算法控制面板 - Summer Sale Campaign                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ 📊 优化目标                                                     │
│ ┌─────────────────────────────────────────────────────────┐   │
│ │ 目标ACoS: [25%] │ 目标ROAS: [4.0] │ 优先级: [利润优先 ▼] │   │
│ └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│ 🎯 自动竞价                                                     │
│ ┌─────────────────────────────────────────────────────────┐   │
│ │ [✓] 启用自动竞价                                         │   │
│ │ 调整幅度: 单次最大 [15%] │ 24小时累计最大 [30%]          │   │
│ │ 最低出价: [$0.15] │ 最高出价: [$3.00]                    │   │
│ └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│ ⏰ 分时策略                                                     │
│ ┌─────────────────────────────────────────────────────────┐   │
│ │ [✓] 启用分时策略                                         │   │
│ │ 策略模式: [自动学习 ▼] │ 调整范围: [-30%] ~ [+50%]       │   │
│ │ [查看时段表现] [手动调整时段]                             │   │
│ └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│ 📍 位置倾斜                                                     │
│ ┌─────────────────────────────────────────────────────────┐   │
│ │ [✓] 启用位置智能倾斜                                     │   │
│ │ Top of Search: [+40%] │ Product Page: [+10%]            │   │
│ │ 调整模式: [自动优化 ▼] │ 调整频率: [每周]                │   │
│ │ [查看位置表现] [手动调整比例]                             │   │
│ └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│ 🚫 自动否词                                                     │
│ ┌─────────────────────────────────────────────────────────┐   │
│ │ [✓] 启用自动否词                                         │   │
│ │ 触发条件: 花费 > [$10] 且 订单 = [0]                     │   │
│ │ 否定类型: [词组否定 ▼] │ 需要审批: [是]                  │   │
│ └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│ [保存设置] [恢复默认] [应用到所有活动]                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 五、执行日志与复盘

### 5.1 实时执行日志

系统记录所有自动执行的操作，用户可随时查看：

```
┌─────────────────────────────────────────────────────────────────┐
│ 📋 算法执行日志 - 今日                                          │
├─────────────────────────────────────────────────────────────────┤
│ 时间        │ 类型     │ 目标           │ 操作        │ 原因    │
├─────────────────────────────────────────────────────────────────┤
│ 14:32:15   │ 竞价调整 │ "wireless..."  │ $0.85→$0.72 │ ACoS>35%│
│ 14:32:15   │ 竞价调整 │ "bluetooth..." │ $0.65→$0.78 │ ACoS<20%│
│ 12:00:00   │ 分时调整 │ 全活动         │ 出价+15%    │ 高峰时段│
│ 10:15:22   │ 否定词   │ "cheap..."     │ 词组否定    │ $12/0单 │
│ 08:00:00   │ 分时调整 │ 全活动         │ 出价-20%    │ 低峰时段│
├─────────────────────────────────────────────────────────────────┤
│ 今日统计: 竞价调整 23次 │ 分时调整 4次 │ 否定词 2个            │
│ [导出日志] [筛选] [查看详情]                                    │
└─────────────────────────────────────────────────────────────────┘
```

### 5.2 效果复盘报告

系统自动在设定的时间节点生成复盘报告：

```
┌─────────────────────────────────────────────────────────────────┐
│ 📊 算法效果复盘 - 过去7天                                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ 整体效果                                                        │
│ ┌─────────────────────────────────────────────────────────┐   │
│ │ 指标      │ 算法执行前 │ 算法执行后 │ 变化   │ 达成率    │   │
│ ├─────────────────────────────────────────────────────────┤   │
│ │ ACoS     │ 32.5%     │ 27.8%     │ -4.7%  │ 112%     │   │
│ │ ROAS     │ 3.08      │ 3.60      │ +0.52  │ 108%     │   │
│ │ 花费     │ $580      │ $520      │ -$60   │ 达成     │   │
│ │ 销售额   │ $1,785    │ $1,872    │ +$87   │ 105%     │   │
│ └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│ 操作统计                                                        │
│ ┌─────────────────────────────────────────────────────────┐   │
│ │ 操作类型     │ 执行次数 │ 成功率 │ 平均效果提升          │   │
│ ├─────────────────────────────────────────────────────────┤   │
│ │ 出价上调     │ 45      │ 89%   │ ROAS +0.3             │   │
│ │ 出价下调     │ 62      │ 92%   │ ACoS -5%              │   │
│ │ 位置倾斜调整 │ 2       │ 100%  │ ROAS +0.5             │   │
│ │ 否定词添加   │ 8       │ 100%  │ 节省花费 $45          │   │
│ └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│ [查看详细报告] [导出PDF] [分享报告]                             │
└─────────────────────────────────────────────────────────────────┘
```

---

## 六、安全机制

### 6.1 边界保护

系统设置多层边界保护，防止异常操作：

| 保护类型 | 规则 | 触发动作 |
|----------|------|----------|
| 出价上限 | 出价不超过设定的最高值 | 截断到最高值 |
| 出价下限 | 出价不低于设定的最低值 | 截断到最低值 |
| 日调整上限 | 24小时内累计调整不超过30% | 暂停后续调整 |
| 预算保护 | 预算调整不超过原预算的50% | 需要人工审批 |
| 高转化保护 | 高转化投放词不自动暂停 | 标记为需审批 |

### 6.2 异常检测与告警

系统实时监控算法执行情况，发现异常立即告警：

| 异常类型 | 检测条件 | 告警方式 |
|----------|----------|----------|
| 花费激增 | 小时花费超过日均的3倍 | 即时通知+暂停算法 |
| 转化骤降 | 转化率下降超过50% | 即时通知 |
| API异常 | 连续3次API调用失败 | 即时通知+暂停执行 |
| 规则冲突 | 同一目标产生矛盾决策 | 记录日志+人工审核 |

### 6.3 回滚机制

所有自动执行的操作都支持回滚：

- 单次操作回滚：恢复单个投放词的出价/状态
- 批量回滚：恢复某次批量操作的所有变更
- 时间点回滚：恢复到指定时间点的状态
- 完全回滚：关闭算法，恢复到算法启用前的状态

---

## 七、实施路线图

### 第一阶段：基础框架（2周）

1. 实现决策引擎核心框架
2. 实现基础的竞价规则
3. 实现执行日志记录
4. 整合到广告活动详情页

### 第二阶段：位置倾斜（1周）

1. 实现位置表现数据分析
2. 实现位置倾斜自动调整算法
3. 实现位置倾斜控制面板

### 第三阶段：分时策略整合（1周）

1. 将分时策略整合到决策引擎
2. 实现分时自动学习算法
3. 整合分时控制到算法面板

### 第四阶段：复盘系统（1周）

1. 实现自动复盘报告生成
2. 实现效果对比分析
3. 实现学习反馈机制

### 第五阶段：优化迭代（持续）

1. 根据用户反馈优化算法
2. 添加更多规则类型
3. 提升决策准确率

---

## 八、预期效果

| 指标 | 当前状态 | 预期效果 |
|------|----------|----------|
| 人工操作频率 | 每日多次 | 每周1-2次监督 |
| 优化响应速度 | 小时级 | 分钟级 |
| 覆盖时段 | 工作时间 | 24/7全天候 |
| 决策一致性 | 依赖个人经验 | 算法标准化 |
| 优化效果 | 基准 | ACoS降低10-20% |

