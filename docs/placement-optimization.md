# 广告位置智能倾斜功能设计

## 一、功能概述

### 背景

Amazon广告提供了三种主要的广告展示位置，每种位置的流量特性、转化率和竞争程度各不相同：

| 位置 | 英文名称 | 特点 |
|------|----------|------|
| 搜索结果顶部 | Top of Search (First Page) | 曝光量大、点击率高、竞争激烈、CPC较高 |
| 商品详情页 | Product Pages | 流量精准、转化率较高、CPC适中 |
| 搜索结果其他位置 | Rest of Search | 曝光量大、点击率较低、CPC较低 |

Amazon允许广告主为不同位置设置出价倍数（Placement Bid Adjustments），范围为0%到900%。合理的位置倾斜策略可以显著提升广告效率。

### 核心价值

位置智能倾斜功能是Adspert等专业广告优化工具的核心竞争力之一。通过自动分析各位置的表现数据，智能调整出价倍数，实现：

1. **最大化ROAS** - 将预算倾斜到转化效率最高的位置
2. **控制ACoS** - 降低低效位置的出价，减少浪费
3. **动态适应** - 根据市场变化自动调整策略
4. **省时省力** - 无需人工分析和手动调整

---

## 二、数据模型

### 2.1 位置表现数据表

```sql
CREATE TABLE placement_performance (
  id INT PRIMARY KEY AUTO_INCREMENT,
  campaign_id VARCHAR(50) NOT NULL,
  placement VARCHAR(50) NOT NULL, -- 'top_of_search', 'product_page', 'rest_of_search'
  date DATE NOT NULL,
  impressions INT DEFAULT 0,
  clicks INT DEFAULT 0,
  spend DECIMAL(10,2) DEFAULT 0,
  sales DECIMAL(10,2) DEFAULT 0,
  orders INT DEFAULT 0,
  -- 计算字段
  ctr DECIMAL(5,4) GENERATED ALWAYS AS (clicks / NULLIF(impressions, 0)),
  cpc DECIMAL(10,2) GENERATED ALWAYS AS (spend / NULLIF(clicks, 0)),
  cvr DECIMAL(5,4) GENERATED ALWAYS AS (orders / NULLIF(clicks, 0)),
  acos DECIMAL(5,4) GENERATED ALWAYS AS (spend / NULLIF(sales, 0)),
  roas DECIMAL(10,2) GENERATED ALWAYS AS (sales / NULLIF(spend, 0)),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  UNIQUE KEY unique_placement_date (campaign_id, placement, date)
);
```

### 2.2 位置倾斜配置表

```sql
CREATE TABLE placement_settings (
  id INT PRIMARY KEY AUTO_INCREMENT,
  campaign_id VARCHAR(50) NOT NULL,
  -- 当前倾斜比例
  top_of_search_adjustment INT DEFAULT 0, -- 0-900%
  product_page_adjustment INT DEFAULT 0,
  -- 自动优化设置
  auto_optimize BOOLEAN DEFAULT TRUE,
  optimization_goal ENUM('roas', 'acos', 'sales', 'profit') DEFAULT 'roas',
  target_acos DECIMAL(5,2) DEFAULT 30.00,
  target_roas DECIMAL(5,2) DEFAULT 3.00,
  -- 调整限制
  min_adjustment INT DEFAULT 0,
  max_adjustment INT DEFAULT 200,
  adjustment_step INT DEFAULT 10, -- 每次调整步长
  adjustment_frequency ENUM('daily', 'weekly', 'biweekly') DEFAULT 'weekly',
  -- 数据要求
  min_clicks_for_decision INT DEFAULT 50, -- 最少点击数才做决策
  min_spend_for_decision DECIMAL(10,2) DEFAULT 20.00,
  -- 时间戳
  last_adjusted_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  UNIQUE KEY unique_campaign (campaign_id)
);
```

### 2.3 位置调整历史表

```sql
CREATE TABLE placement_adjustment_history (
  id INT PRIMARY KEY AUTO_INCREMENT,
  campaign_id VARCHAR(50) NOT NULL,
  placement VARCHAR(50) NOT NULL,
  previous_adjustment INT NOT NULL,
  new_adjustment INT NOT NULL,
  reason TEXT,
  -- 调整时的数据快照
  snapshot_impressions INT,
  snapshot_clicks INT,
  snapshot_spend DECIMAL(10,2),
  snapshot_sales DECIMAL(10,2),
  snapshot_acos DECIMAL(5,4),
  snapshot_roas DECIMAL(10,2),
  -- 预期效果
  expected_acos_change DECIMAL(5,4),
  expected_roas_change DECIMAL(10,2),
  -- 实际效果（复盘时填充）
  actual_acos_change DECIMAL(5,4),
  actual_roas_change DECIMAL(10,2),
  reviewed_at TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

---

## 三、算法设计

### 3.1 位置效率评分算法

系统为每个位置计算效率评分，用于决定倾斜方向和幅度。

**评分公式**：

```
效率评分 = (ROAS权重 × ROAS得分) + (ACoS权重 × ACoS得分) + (CVR权重 × CVR得分)
```

其中：
- ROAS得分 = 位置ROAS / 活动平均ROAS
- ACoS得分 = 活动平均ACoS / 位置ACoS（ACoS越低越好，所以取倒数）
- CVR得分 = 位置CVR / 活动平均CVR

**权重配置**（根据优化目标调整）：

| 优化目标 | ROAS权重 | ACoS权重 | CVR权重 |
|----------|----------|----------|---------|
| ROAS优先 | 0.5 | 0.3 | 0.2 |
| ACoS优先 | 0.3 | 0.5 | 0.2 |
| 销售额优先 | 0.4 | 0.2 | 0.4 |
| 利润优先 | 0.4 | 0.4 | 0.2 |

### 3.2 倾斜比例计算算法

基于效率评分计算建议的倾斜比例：

```typescript
interface PlacementScore {
  placement: 'top_of_search' | 'product_page' | 'rest_of_search';
  efficiencyScore: number;
  currentAdjustment: number;
  suggestedAdjustment: number;
  confidence: number;
}

function calculateSuggestedAdjustment(
  scores: PlacementScore[],
  settings: PlacementSettings
): PlacementScore[] {
  // 1. 找到效率最高的位置作为基准
  const maxScore = Math.max(...scores.map(s => s.efficiencyScore));
  const minScore = Math.min(...scores.map(s => s.efficiencyScore));
  
  // 2. 计算相对效率
  return scores.map(score => {
    const relativeEfficiency = (score.efficiencyScore - minScore) / (maxScore - minScore);
    
    // 3. 映射到调整范围
    // 效率最高的位置获得最大倾斜，效率最低的位置倾斜为0
    const targetAdjustment = Math.round(
      relativeEfficiency * settings.maxAdjustment
    );
    
    // 4. 限制单次调整幅度
    const currentAdj = score.currentAdjustment;
    const maxChange = settings.adjustmentStep * 2; // 单次最大变化
    
    let suggestedAdjustment = targetAdjustment;
    if (Math.abs(targetAdjustment - currentAdj) > maxChange) {
      suggestedAdjustment = currentAdj + 
        Math.sign(targetAdjustment - currentAdj) * maxChange;
    }
    
    // 5. 确保在允许范围内
    suggestedAdjustment = Math.max(
      settings.minAdjustment,
      Math.min(settings.maxAdjustment, suggestedAdjustment)
    );
    
    return {
      ...score,
      suggestedAdjustment,
      confidence: calculateConfidence(score)
    };
  });
}

function calculateConfidence(score: PlacementScore): number {
  // 置信度基于数据量
  // 点击数越多，置信度越高
  const clicksConfidence = Math.min(1, score.clicks / 100);
  const spendConfidence = Math.min(1, score.spend / 50);
  return (clicksConfidence + spendConfidence) / 2;
}
```

### 3.3 自动调整决策流程

```
┌─────────────────────────────────────────────────────────────────┐
│                     位置倾斜自动调整流程                          │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │ 检查调整频率    │
                    │ 是否到达调整时间 │
                    └────────┬────────┘
                             │
              ┌──────────────┴──────────────┐
              │ 否                          │ 是
              ▼                             ▼
        ┌──────────┐              ┌─────────────────┐
        │ 跳过本次  │              │ 获取位置表现数据 │
        └──────────┘              └────────┬────────┘
                                           │
                                           ▼
                                 ┌─────────────────┐
                                 │ 检查数据是否充足 │
                                 │ (点击数/花费)   │
                                 └────────┬────────┘
                                          │
                           ┌──────────────┴──────────────┐
                           │ 否                          │ 是
                           ▼                             ▼
                     ┌──────────┐              ┌─────────────────┐
                     │ 跳过本次  │              │ 计算效率评分    │
                     │ 记录原因  │              └────────┬────────┘
                     └──────────┘                        │
                                                         ▼
                                               ┌─────────────────┐
                                               │ 计算建议倾斜比例 │
                                               └────────┬────────┘
                                                        │
                                                        ▼
                                               ┌─────────────────┐
                                               │ 与当前比例对比   │
                                               │ 是否需要调整    │
                                               └────────┬────────┘
                                                        │
                                         ┌──────────────┴──────────────┐
                                         │ 否                          │ 是
                                         ▼                             ▼
                                   ┌──────────┐              ┌─────────────────┐
                                   │ 保持不变  │              │ 执行调整        │
                                   └──────────┘              │ 记录历史        │
                                                             │ 设置复盘时间    │
                                                             └─────────────────┘
```

---

## 四、界面设计

### 4.1 位置表现概览卡片

在广告活动详情页显示位置表现概览：

```
┌─────────────────────────────────────────────────────────────────┐
│ 📍 广告位置表现分析                                [自动优化: 开] │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 位置           │ 倾斜  │ 花费   │ 销售额 │ ACoS  │ ROAS │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 🔝 搜索顶部    │ +40% │ $320  │ $1,280 │ 25.0% │ 4.00 │   │
│  │ 📦 商品详情页  │ +10% │ $180  │ $630   │ 28.6% │ 3.50 │   │
│  │ 📋 其他搜索位置│ 0%   │ $100  │ $280   │ 35.7% │ 2.80 │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  效率对比图                                                     │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │     ROAS                                                │   │
│  │  4.0 ████████████████████████████████████ 搜索顶部      │   │
│  │  3.5 ██████████████████████████████ 商品详情页          │   │
│  │  2.8 ████████████████████ 其他搜索位置                  │   │
│  │      0    1    2    3    4    5                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  💡 AI建议: 搜索顶部ROAS最高(4.0)，建议维持当前+40%倾斜策略     │
│                                                                 │
│  [查看详细分析] [手动调整] [查看历史]                           │
└─────────────────────────────────────────────────────────────────┘
```

### 4.2 位置倾斜设置面板

```
┌─────────────────────────────────────────────────────────────────┐
│ ⚙️ 位置倾斜设置                                                 │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ 优化模式                                                        │
│ ┌─────────────────────────────────────────────────────────┐   │
│ │ ○ 手动模式 - 手动设置各位置倾斜比例                       │   │
│ │ ● 自动模式 - 系统根据表现数据自动调整                     │   │
│ └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│ 自动优化设置                                                    │
│ ┌─────────────────────────────────────────────────────────┐   │
│ │ 优化目标: [ROAS优先 ▼]                                   │   │
│ │ 目标ROAS: [3.5]  │ 目标ACoS: [28%]                       │   │
│ │                                                          │   │
│ │ 调整频率: [每周 ▼]                                       │   │
│ │ 调整步长: [10%]  │ 最大倾斜: [200%]                      │   │
│ │                                                          │   │
│ │ 数据要求:                                                │   │
│ │ 最少点击数: [50]  │ 最少花费: [$20]                      │   │
│ └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│ 当前倾斜比例                                                    │
│ ┌─────────────────────────────────────────────────────────┐   │
│ │ 🔝 搜索顶部:     [====●===========] 40%                  │   │
│ │ 📦 商品详情页:   [==●=============] 10%                  │   │
│ │ 📋 其他搜索位置: (基准，无法调整)                         │   │
│ └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│ [保存设置] [重置为默认] [立即执行优化]                          │
└─────────────────────────────────────────────────────────────────┘
```

### 4.3 位置表现趋势图

```
┌─────────────────────────────────────────────────────────────────┐
│ 📈 位置表现趋势 - 过去30天                    [7天|14天|30天|60天]│
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ROAS趋势                                                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 5.0│                    ╭─╮                              │   │
│  │    │        ╭──╮   ╭──╯  ╰──╮    ╭──────── 搜索顶部     │   │
│  │ 4.0│   ╭──╯    ╰──╯         ╰──╯                        │   │
│  │    │  ╭╯                         ╭────────── 商品详情页  │   │
│  │ 3.0│ ╭╯    ╭──────────────────────╯                     │   │
│  │    │╭╯╭────╯                      ╭─────────── 其他位置  │   │
│  │ 2.0│╯╭╯                      ╭────╯                      │   │
│  │    ├─┴───────────────────────┴───────────────────────── │   │
│  │    │ 12/1  12/7  12/14  12/21  12/28  1/4               │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  倾斜比例调整历史                                               │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 日期    │ 位置     │ 调整前 │ 调整后 │ 原因              │   │
│  ├─────────────────────────────────────────────────────────┤   │
│  │ 1/1    │ 搜索顶部 │ 30%   │ 40%   │ ROAS提升至4.0     │   │
│  │ 12/25  │ 商品详情 │ 20%   │ 10%   │ ROAS下降至3.2     │   │
│  │ 12/18  │ 搜索顶部 │ 20%   │ 30%   │ ROAS持续高于平均  │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  [导出数据] [查看完整历史]                                      │
└─────────────────────────────────────────────────────────────────┘
```

---

## 五、API设计

### 5.1 获取位置表现数据

```typescript
// GET /api/trpc/campaign.getPlacementPerformance
interface GetPlacementPerformanceInput {
  campaignId: string;
  startDate: string;
  endDate: string;
}

interface PlacementPerformance {
  placement: 'top_of_search' | 'product_page' | 'rest_of_search';
  impressions: number;
  clicks: number;
  spend: number;
  sales: number;
  orders: number;
  ctr: number;
  cpc: number;
  cvr: number;
  acos: number;
  roas: number;
  currentAdjustment: number;
}

interface GetPlacementPerformanceOutput {
  placements: PlacementPerformance[];
  campaignAverage: {
    acos: number;
    roas: number;
    ctr: number;
    cvr: number;
  };
  lastAdjustedAt: string | null;
}
```

### 5.2 更新位置倾斜设置

```typescript
// POST /api/trpc/campaign.updatePlacementSettings
interface UpdatePlacementSettingsInput {
  campaignId: string;
  topOfSearchAdjustment?: number;
  productPageAdjustment?: number;
  autoOptimize?: boolean;
  optimizationGoal?: 'roas' | 'acos' | 'sales' | 'profit';
  targetAcos?: number;
  targetRoas?: number;
  adjustmentFrequency?: 'daily' | 'weekly' | 'biweekly';
  minAdjustment?: number;
  maxAdjustment?: number;
}
```

### 5.3 执行位置优化

```typescript
// POST /api/trpc/campaign.optimizePlacements
interface OptimizePlacementsInput {
  campaignId: string;
  dryRun?: boolean; // 仅计算建议，不实际执行
}

interface OptimizePlacementsOutput {
  suggestions: {
    placement: string;
    currentAdjustment: number;
    suggestedAdjustment: number;
    reason: string;
    expectedImpact: {
      acos: number;
      roas: number;
    };
    confidence: number;
  }[];
  executed: boolean;
  executionId?: string;
}
```

### 5.4 获取位置调整历史

```typescript
// GET /api/trpc/campaign.getPlacementHistory
interface GetPlacementHistoryInput {
  campaignId: string;
  limit?: number;
}

interface PlacementAdjustmentRecord {
  id: number;
  placement: string;
  previousAdjustment: number;
  newAdjustment: number;
  reason: string;
  snapshotAcos: number;
  snapshotRoas: number;
  expectedAcosChange: number;
  expectedRoasChange: number;
  actualAcosChange: number | null;
  actualRoasChange: number | null;
  createdAt: string;
  reviewedAt: string | null;
}
```

---

## 六、与现有功能整合

### 6.1 整合到广告活动详情页

位置表现分析将作为广告活动详情页的一个重要模块，与现有的投放词、搜索词、AI分析等模块并列展示。

```
┌─────────────────────────────────────────────────────────────────┐
│ 广告活动详情 - Summer Sale Campaign                             │
├─────────────────────────────────────────────────────────────────┤
│ [概览] [投放词] [搜索词] [📍位置分析] [否定词] [AI分析] [设置]   │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  （位置分析Tab内容）                                            │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 6.2 整合到算法控制面板

位置智能倾斜作为算法自主决策引擎的一个子模块，在算法控制面板中统一管理：

```
┌─────────────────────────────────────────────────────────────────┐
│ 🤖 算法控制面板                                                 │
├─────────────────────────────────────────────────────────────────┤
│ ✅ 自动竞价      │ 已启用 │ 今日调整 5 个投放词                 │
│ ✅ 分时策略      │ 已启用 │ 当前时段出价 +15%                   │
│ ✅ 位置智能倾斜  │ 已启用 │ Top: +40% │ PP: +10%               │
│ ✅ 自动否词      │ 已启用 │ 本周否定 3 个搜索词                 │
└─────────────────────────────────────────────────────────────────┘
```

### 6.3 整合到AI智能分析

AI分析结果中将包含位置优化建议：

```
┌─────────────────────────────────────────────────────────────────┐
│ 💡 AI优化建议                                                   │
├─────────────────────────────────────────────────────────────────┤
│ 📍 位置优化建议                                                 │
│ • 搜索顶部ROAS(4.2)显著高于平均(3.5)，建议提高倾斜至+50%        │
│ • 商品详情页ROAS(2.8)低于平均，建议降低倾斜至0%                 │
│ 预期效果: ACoS降低3%，ROAS提升0.3                              │
│ [一键执行] [查看详情]                                           │
└─────────────────────────────────────────────────────────────────┘
```

---

## 七、实施计划

### 第一阶段：数据基础（3天）

1. 创建位置表现数据表
2. 实现位置数据同步（从Amazon API获取）
3. 实现位置表现查询API

### 第二阶段：分析算法（3天）

1. 实现效率评分算法
2. 实现倾斜比例计算算法
3. 实现自动调整决策逻辑

### 第三阶段：界面开发（4天）

1. 开发位置表现概览卡片
2. 开发位置倾斜设置面板
3. 开发位置表现趋势图
4. 整合到广告活动详情页

### 第四阶段：自动化整合（2天）

1. 整合到算法控制面板
2. 整合到AI智能分析
3. 实现定时自动优化任务

### 第五阶段：测试优化（2天）

1. 功能测试
2. 算法效果验证
3. 性能优化

---

## 八、预期效果

| 指标 | 预期改善 |
|------|----------|
| ACoS | 降低5-15% |
| ROAS | 提升10-25% |
| 人工操作时间 | 减少80% |
| 响应速度 | 从周级提升到日级 |

