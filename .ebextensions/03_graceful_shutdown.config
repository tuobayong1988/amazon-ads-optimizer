## ============================================================================
## 03_graceful_shutdown.config - 优雅关闭和健康检查配置
## ============================================================================
## 
## ⚠️  安全审查警告 ⚠️
## 对此文件的任何修改都可能导致生产环境部署失败或服务中断！
## 
## 禁止修改的配置项（v666事故教训）：
##   - 禁止添加 IgnoreHealthCheck: true（会导致崩溃的应用不被自动回滚）
##   - 禁止设置 HealthCheckSuccessThreshold: Degraded（会降低部署安全阈值）
##   - 禁止修改 DeploymentPolicy 为 AllAtOnce（会导致零停机保障失效）
##
## 修改此文件前必须：
##   1. 在测试环境验证配置变更
##   2. 确认健康检查端点 /health 正常响应
##   3. 获得团队审批
##
## 最后安全审查：v668 (2026-04-14)
## 稳定基线版本：v664
## ============================================================================

option_settings:
  ## 健康检查配置 - 使用 /health 端点
  aws:elasticbeanstalk:application:
    Application Healthcheck URL: /health
  
  ## 增强型健康检查
  aws:elasticbeanstalk:healthreporting:system:
    SystemType: enhanced
  
  ## 部署策略 - 滚动部署，确保零停机
  ## ⚠️ 不要修改为 AllAtOnce，不要添加 IgnoreHealthCheck
  aws:elasticbeanstalk:command:
    DeploymentPolicy: Rolling
    BatchSize: 1
    Timeout: 600
  
  ## 环境变量 - 设置Node.js优雅关闭超时
  aws:elasticbeanstalk:application:environment:
    ## 告诉Node.js在SIGTERM后等待的最大时间（秒）
    GRACEFUL_SHUTDOWN_TIMEOUT: 90
