## v665: 优雅关闭和健康检查配置
## 确保部署时给应用足够时间完成正在执行的同步/优化任务
## v665更新: 增加健康检查容忍度，避免大账户同步期间因内存峰值触发EB重启

option_settings:
  ## 健康检查配置 - 使用 /health 端点
  aws:elasticbeanstalk:application:
    Application Healthcheck URL: /health
  
  ## 增强型健康检查 - v665: 增加容忍度
  aws:elasticbeanstalk:healthreporting:system:
    SystemType: enhanced
    ## v665: 健康检查评估间隔和容忍度
    ## HealthCheckSuccessThreshold: 降低阈值，允许degraded状态不触发重启
    ## Ok = 只有完全不响应才认为不健康
    HealthCheckSuccessThreshold: Degraded

  ## ELB 健康检查配置 - v665: 增加容忍度
  aws:elasticbeanstalk:environment:process:default:
    HealthCheckPath: /health
    HealthCheckInterval: 30
    HealthCheckTimeout: 10
    HealthyThresholdCount: 2
    UnhealthyThresholdCount: 10
    
  ## 部署策略 - 滚动部署，确保零停机
  aws:elasticbeanstalk:command:
    DeploymentPolicy: Rolling
    BatchSize: 1
    Timeout: 600
    IgnoreHealthCheck: true
  
  ## 环境变量 - 设置Node.js优雅关闭超时
  aws:elasticbeanstalk:application:environment:
    ## 告诉Node.js在SIGTERM后等待的最大时间（秒）
    GRACEFUL_SHUTDOWN_TIMEOUT: 90
