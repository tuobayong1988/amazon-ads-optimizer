# v26.15.0 系统审计修复报告

**版本**: v26.15.0 (app-v696)
**部署时间**: 2026-04-25
**修复范围**: 系统全面审计后的P0/P1/P2修复

## 修复清单

### P0 — 步骤引擎依赖链断裂（严重）

**根因**：`initializeStepRuns` 函数在全量运行模式下，通过 `db.delete(stepRuns).where(eq(stepRuns.projectId, projectId))` 全量删除所有步骤记录后重建，导致已完成步骤的状态丢失。当步骤引擎在 `getExecutableSteps` 中检查依赖时，因前置步骤记录不存在而将下游步骤标记为 blocked。

**修复方案**：重构为**增量合并模式**。保留 completed/skipped 状态的步骤记录不变，仅对 pending/blocked/failed 状态的步骤进行重置。新增步骤时插入 pending 状态，不影响已有记录。

**代码位置**：`dist/index.js` 第30697行附近

**数据修复**：通过SQL将84个历史 blocked 步骤重置为 pending，使步骤引擎在下次运行时能正确执行。

### P1 — 调研阶段广告渠道数据缺失（高优先级）

**根因**：`RESEARCH_STEPS` 和 `PRELAUNCH_STEPS` 配置中，`D0_CHANNEL_GAP` 步骤被设置为 `skip: true`，导致调研和预发布阶段无法采集 SP/SB/SBV 广告渠道数据。竞争饱和度惩罚机制因缺少广告数据而无法在这两个阶段生效。

**修复方案**：将 `D0_CHANNEL_GAP` 在 `RESEARCH_STEPS` 和 `PRELAUNCH_STEPS` 中的 `skip` 标志改为 `false`。

**代码位置**：`dist/index.js` 第30569行和第30594行附近

### P2 — 报告导出能力增强（中优先级）

**新增功能**：`exportCenter.exportDecisionReport` 端点，生成包含6个Sheet的决策级Excel报告。

各Sheet内容如下：

| Sheet名称 | 内容 |
|-----------|------|
| 决策摘要 | 项目基本信息、关键词智能汇总、竞争格局汇总 |
| 关键词智能 | 完整关键词数据（搜索量、购买量、CPC、层级、价值分、相关度、搜索意图等18列） |
| 竞争格局 | 所有竞品详情（ASIN、标题、品牌、价格、评分、评论数、月销量等14列） |
| 定价分析 | 定价策略数据（目标价、最低价、最高价、盈亏平衡价等11列） |
| VOC与用户画像 | 用户画像数据（画像名称、描述、核心需求、痛点等8列） |
| 执行状态 | 所有步骤的执行状态（步骤ID、状态、开始时间、完成时间、错误信息等6列） |

**S3 Fallback**：`uploadExcel` 函数增加了双模式支持。优先使用 S3 上传，当 S3 未配置时自动 fallback 为 Base64 data URL 返回。

**代码位置**：`dist/index.js` 第18797行（uploadExcel）和第27176行（exportDecisionReport）

## 验证结果

| 验证项 | 结果 |
|--------|------|
| 品牌词库统计卡片 | 正常（2,456 / 5,878 / 386 / $1.31 / 13） |
| 决策级报告导出 | 成功生成42KB Excel，6个Sheet数据完整 |
| 步骤引擎blocked修复 | 84个blocked步骤已重置为pending |
| 网站整体运行 | 正常，无白屏无报错 |

## 累计版本迭代

| 版本 | 核心修复 |
|------|----------|
| v26.12.0 | 令牌桶限速器 + 竞品分层采样 + 步骤时间展示 |
| v26.13.0 | 盈亏平衡模型 + VOC分析恢复 + D6品类趋势 + 红线拦截 |
| v26.13.1 | 成本缺失防误判 + D6降级标记 + 队列深度限制 + 前端崩溃修复 |
| v26.14.0 | 词库购买量列 + 竞争饱和度惩罚 + 词频校验 + 参数前置策略 |
| v26.14.1 | 品牌词库统计卡片修复（db.execute返回值解析） |
| v26.15.0 | 步骤引擎依赖链修复 + 调研阶段广告渠道启用 + 决策级报告导出 |
