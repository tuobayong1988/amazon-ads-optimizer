# AmzOrbit v26.12.0 (app-v692) 变更日志

**发布日期**: 2026-04-25
**部署环境**: prelaunch-engine / prelaunch-prod-lb
**基础版本**: v26.11.0e (app-v691e)

---

## 三项核心优化

### 优化1：智能API速率控制和排队机制

在SellerSprite API调用层引入了全局令牌桶（Token Bucket）限速器，解决多个并发引擎运行时API配额竞争导致的频繁rate limit问题。

| 配置项 | 值 | 说明 |
|--------|------|------|
| maxTokens | 10 | 令牌桶容量 |
| refillRate | 10/min | 每分钟补充令牌数 |
| 队列机制 | FIFO | 超出限额的请求自动排队等待 |

**实现细节**：在 `callTool` 函数中，熔断器检查通过后、实际API调用前，新增了令牌获取逻辑。当令牌不足时，请求自动进入队列等待，由后台定时器每秒检查并分配令牌。日志输出 `[SellerSprite:RL]` 标记的排队信息，便于监控。

### 优化2：非核心竞品数据采样策略

在 `handleKeywordCollect` 函数中引入分层采样策略，将竞品按BSR排名分为三个层级，大幅减少API调用量。

| 层级 | 范围 | 采样率 | 说明 |
|------|------|--------|------|
| T1 | 前20个竞品 | 100% | 核心竞品，全量反查 |
| T2 | 第21-50个竞品 | 50% | 中等竞品，隔一采样 |
| T3 | 第51个及以后 | 0% | 长尾竞品，跳过 |
| 总上限 | 50个ASIN | - | 包含seedAsin在内 |

**效果预估**：对于拥有200+竞品的项目，API调用量从 600+ 降至约 150，减少约75%。引擎运行时间从90分钟级别降至30分钟级别。

### 优化3：前端步骤状态展示与预计完成时间

**后端改进**：

在 `getStatus` 端点中新增两个字段：`startedAt`（引擎启动时间）和 `dynamicEstimate`（基于实际进度动态计算的剩余时间）。动态估算逻辑为：已运行时间除以已完成步骤数，得到每步平均耗时，再乘以剩余步骤数。

**STEP_METADATA 时间修正**：

| 步骤 | 旧估算(分钟) | 新估算(分钟) | 实测范围 |
|------|-------------|-------------|---------|
| CHART_SCAN | 8 | 12 | 8-15 |
| SIMILAR_IDENTIFY | 3 | 8 | 5-12 |
| KEYWORD_COLLECT | 5 | 45 | 30-90 |
| SEARCH_EXPAND | 5 | 15 | 10-25 |
| REVIEWS | 8 | 20 | 15-30 |

**前端改进**：

StepEnginePanel组件的时间显示从静态的"预计 ~X 分钟"改为动态的"已运行 Xh Ym · 剩余 ~Xh Ym"格式。当引擎运行中且有动态估算数据时，显示实际已运行时间和基于进度的剩余时间预估；引擎未运行时，仍显示静态估算值。

---

## 验证结果

部署后在 Physical Therapy Leg Exercisers 项目上验证，前端正确显示"已运行 2h 32m · 剩余 ~6h 21m"，确认三项优化均已生效。网站正常运行，无白屏或崩溃问题。

## 文件修改清单

| 文件 | 修改内容 |
|------|---------|
| dist/index.js | 令牌桶限速器、竞品采样、estimatedMinutes修正、getStatus新增字段 |
| dist/public/assets/StepEnginePanel-CMKaOWQL.js | 动态时间显示逻辑 |
| dist/public/assets/StepEnginePanel-CMKaOWQL.js.gz | 重新压缩 |
| package.json | 版本号 26.11.0 → 26.12.0 |
| server/utils/systemVersion.ts | SYSTEM_VERSION 691 → 692 |
