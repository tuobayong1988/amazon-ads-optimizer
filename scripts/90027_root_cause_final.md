# 账户90027根因分析 — 最终结论

## 排查时间: 2026-03-06 09:30 UTC

## 核心发现：syncAll任务完成但记录数为0

### 关键对比

| 指标 | 账户90023（正常） | 账户90027（异常） |
|:---|:---|:---|
| syncAll记录数 | 44,153行 | **0行** |
| 绩效最新日期 | 2026-03-06 | 2026-03-03 |
| V339后新增绩效 | 6,701行 | **0行** |
| 搜索词最后更新 | 2026-03-06 | 2026-02-23 |
| bid_decrease失败 | 0次 | 17次(401错误) |

### 根因定位

**问题1（P0）：syncAll完成但recordsSynced=0**

账户90027的syncAll任务在V339部署后执行了多次（07:58、08:59），每次都标记为"completed"，但实际同步的记录数为0。这意味着syncAll方法内部在处理90027时遇到了某种静默错误，导致所有数据同步步骤被跳过，但没有抛出异常。

可能的原因：
1. API报告请求返回空数据（Token虽然能刷新用于读取，但报告API可能有不同的权限要求）
2. 90027有3360个广告活动（远多于90023的2936个），其中97.4%是paused/archived，可能导致报告请求超时或被限流
3. syncAll内部的try-catch捕获了异常但没有正确记录

**问题2（P0）：API写操作401认证失败**

所有6个凭证共享同一个Refresh Token（Atzr|IwEBIMm8gAsH_oJG7ey4dKTpI...），但401错误分布在所有3个账户中（90021: 6次, 90023: 45次, 90027: 18次）。这说明不是90027独有的问题，而是Refresh Token在某些时刻刷新Access Token失败。

但90023的bid_decrease成功率极高（25,588成功/0失败），而90027有17次失败。差异可能在于：
- 90027的写操作总量少（278次），失败17次=6.1%的失败率
- 90023的写操作量大（26,834次），45次401错误可能被自动重试成功了

**问题3：绩效数据仅49天（1/16~3/3）**

2026-01-16距离账户创建日（2026-02-03）有18天。这意味着初始化同步时请求了约49天的数据（从1/16到2/23初始化完成），而不是预期的90天。这是因为syncAll中的performanceDays在V339之前被硬编码为14天，而初始化流程可能使用了不同的天数参数。

## 修复建议

### 立即行动

1. **排查syncAll返回0记录的原因**：在syncAll方法中增加更详细的日志，记录每个同步步骤的开始、结束和记录数，以及任何被捕获的异常。

2. **手动触发90027的full tier同步**：通过API或直接调用，强制执行一次带90天参数的完整同步，观察是否能成功拉取数据。

3. **检查API Token刷新机制**：确认Refresh Token在刷新Access Token时是否有竞态条件（6个凭证共享同一个Refresh Token，可能导致并发刷新冲突）。

### 后续优化

4. **增加同步健康监控**：当syncAll完成但recordsSynced=0时，应该触发告警而不是静默通过。

5. **分离大账户的同步策略**：90027有3360个广告活动，应该考虑分批同步而不是一次性处理所有活动。
