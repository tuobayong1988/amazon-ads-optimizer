# v26.14.1 品牌词库统计卡片修复报告

## 版本信息
- **版本**: v26.14.1 (app-v695)
- **部署时间**: 2026-04-25
- **环境**: prelaunch-prod-lb

## 问题描述

品牌团队 → 品牌详情 → 品牌词库 tab 中的统计卡片（独立关键词、平均搜索量、平均月购买量、平均CPC）全部显示为 **0**，而"覆盖产品数"正常显示为 **13**。

## 根因分析

### Bug 位置
`dist/index.js` 第 31988-31995 行，`getKeywordAggregation` tRPC 端点。

### 技术细节

该端点使用 `db.execute()` 执行原生 SQL 聚合查询：

```sql
SELECT COUNT(DISTINCT keyword) as total_kw,
       AVG(search_volume) as avg_sv,
       AVG(purchase_volume) as avg_pv,
       AVG(suggested_cpc) as avg_cpc
FROM keywords
WHERE project_id IN (...)
```

**Drizzle ORM (v0.44.5) + mysql2** 的 `db.execute()` 返回格式为 `[rowsArray, fieldsArray]`（mysql2 原生格式），但代码中：

```javascript
const summaryRows = await db.execute(summaryQuery);
const summaryRow = summaryRows[0];  // ← BUG: 这取到的是整个 rows 数组，不是第一行
```

`summaryRows[0]` 实际上是 `[{total_kw: 2456, avg_sv: 5878, ...}]`（一个数组），而非 `{total_kw: 2456, ...}`（一个对象）。因此 `summaryRow?.total_kw` 为 `undefined`，最终所有值都被 fallback 为 0。

"覆盖产品数 = 13" 正常显示是因为它来自 `teamProjects.length`（JavaScript 数组长度），不依赖该 SQL 结果。

### 修复方案

```javascript
// 修复前
const summaryRow = summaryRows[0];

// 修复后
const summaryRow = summaryRows[0]?.[0] ?? summaryRows[0];
```

增加 `[0][0]` 的二次索引，正确提取 `[rows, fields]` 中 rows 数组的第一行。使用 `??` 运算符保持向后兼容。

## 验证结果

### 修复前
| 指标 | 值 |
|------|-----|
| 独立关键词 | 0 |
| 平均搜索量 | 0 |
| 平均月购买量 | 0 |
| 平均CPC | $0.00 |
| 覆盖产品数 | 13 |

### 修复后
| 指标 | 值 |
|------|-----|
| 独立关键词 | **2,456** |
| 平均搜索量 | **5,878** |
| 平均月购买量 | **386** |
| 平均CPC | **$1.31** |
| 覆盖产品数 | **13** |

### 数据库直查验证
```
total_kw=2456, avg_sv=5878, avg_pv=386, avg_cpc=1.31
```
前端显示结果与数据库直查完全匹配。

## 影响范围
- 品牌团队 → 品牌详情 → 品牌词库 tab 的统计卡片
- 修复仅涉及 `getKeywordAggregation` 端点的返回值解析
- 不影响其他模块
