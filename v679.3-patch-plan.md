# v679.3 补丁方案

## 改动总览

### 1. Import新增
- 添加 `import { AutoOptimizeGuard } from '../services/autoOptimizeGuard';`

### 2. 主循环改造 (行316-372)
- 删除50行手动SQL查询 + `__v679_disabled_campaign_ids` 注入
- 替换为: `const guard = await AutoOptimizeGuard.create(accId, 'auto_correction');`
- 使用 `guard.isAccountFullyDisabled()` 替代 `allDisabled` 检查
- 使用 `guard.getDisabledPgNames()` 替代手动PG名称提取

### 3. 子函数签名变更
所有15个子函数需要接收 `guard: AutoOptimizeGuard` 参数（替代从database读取disabled IDs）

受影响的子函数（8处有v679过滤的）:
- retryFailedBidAdjustments (行616)
- correctBidMismatches (行1177)  
- retryFailedBudgetAdjustments (行1583)
- retryFailedSettingsChanges (行1702)
- retryFailedKeywordCreations (行1913)
- retryFailedNegativeKeywordAdds (行2095)
- retryHistoricalFailedKeywordHarvests (行3085)
- retryFailedTargetStatusChanges (行4400)

不受影响的子函数（无v679过滤，但SQL中已有auto_optimize=1条件或不涉及campaign过滤）:
- cleanupExpiredDaypartingBids
- correctMaxBidViolations (SQL已有pg.auto_optimize=1)
- cleanupOrphanKeywords
- rescuePermanentlyFailedTasks
- backfillNegativeKeywordIds
- verifyBiddingLogsExecution
- auditAlgorithmDecisionQuality (SQL已有pg.auto_optimize=1)
- correctPlacementMismatches
- executeUnfinishedRollbacks
- retryFailedProductTargetCreations (SQL已有pg.auto_optimize=1)
- revalidateStalePendingCommands (SQL已有pg.auto_optimize=1)

### 4. 审计日志增强
- 在主循环中每个子函数执行后，对其返回的corrections立即调用recordAudit
- 增加 `source: 'auto_correction'` 标识（替代原来的 `source: 'system'`）
- 对失败的corrections也记录审计（原来只记录成功的）
- 对被Guard拦截的操作，通过guard.recordBlockedOperation记录

### 5. 删除旧的批量审计 (行487-501)
- 删除主循环末尾的 `for (const correction of corrections)` 批量审计循环
- 替换为在每个子函数调用后的实时审计

## v679过滤替换模式

旧模式（每个子函数内部）:
```typescript
// @ts-ignore v679: runtime context injection
const disabledCids = (database as any).__v679_disabled_campaign_ids as Set<number> | null;
const filteredEvents = disabledCids 
  ? failedEvents.filter((e: any) => !disabledCids.has(Number(e.campaignId)))
  : failedEvents;
```

新模式:
```typescript
const filteredEvents = guard 
  ? failedEvents.filter((e: any) => guard.isCampaignAllowed(Number(e.campaignId || e.campaign_id)))
  : failedEvents;
```
