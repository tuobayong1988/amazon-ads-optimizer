# Amazon Ads Optimizer v389 深度评估与优化方案

**文档版本**: 1.0
**日期**: 2026-03-10

## 1. 概述

本方案基于对 Amazon Ads Optimizer v388 版本的全面排查，结合 v379 版本的历史优化建议，旨在解决当前版本中存在的P1级（高优先级）、P2级（中优先级）和P3级（低优先级）问题。优化核心目标是提升系统在200-500租户规模下的**可扩展性、稳定性、数据准确性及资源利用率**，确保系统能够长期、可靠、高效地为大规模用户提供服务。

## 2. 核心问题分析 (v388 排查结论)

经过对前端、后端、架构及配置的全面排查，我们识别出以下关键问题：

| 优先级 | 问题类别 | 问题描述 | 影响分析 |
| :--- | :--- | :--- | :--- |
| **P1** | **功能缺失** | SD广告否定定位同步逻辑 (`syncSdNegativeTargets`) 已实现但未在主同步引擎 (`unifiedSyncEngine.ts`) 中注册。 | 系统无法同步SD广告的否定定位数据，导致基于此的优化策略（如智能否ASIN）完全失效。 |
| **P1** | **配置错误** | Elastic Beanstalk 实例 (`t3.small`, 2GB RAM) 的Node.js内存限制 (`--max-old-space-size=2048`) 设置等于甚至超过实例物理内存。 | 极易导致实例因内存不足而被操作系统强制终止进程，引发服务频繁中断和重启，严重影响系统稳定性。 |
| **P2** | **性能瓶颈** | EB实例类型 `t3.small` 对于承载大规模（200-500）租户的并发同步和优化计算任务严重不足。 | 在高负载下，CPU和内存将成为瓶颈，导致任务处理延迟、超时和失败率增高，无法满足服务等级协议 (SLA)。 |
| **P2** | **功能验证** | “纠错监控” (`/auto-correction`) 页面在生产环境加载失败，前端路由正常但后端数据显示异常。 | 用户无法使用该功能监控和审查系统的自动纠错行为，影响系统的透明度和可信度。 |
| **P3** | **配置优化** | 数据库连接池大小 (`connectionLimit: 25`) 和并发账户同步数 (`MAX_CONCURRENT_ACCOUNTS: 10`) 配置相对保守。 | 虽然当前配置稳定，但在实例规格升级后，有进一步提升并发处理能力的空间，以提高大规模租户的同步效率。 |

## 3. v389 详细优化方案

### 3.1. P1 级问题修复 (最高优先级)

#### 3.1.1. 注册SD否定定位同步模块
- **目标**: 补全SD广告的数据同步能力。
- **实施步骤**:
  1.  **编辑 `server/unifiedSyncEngine.ts` 文件**。
  2.  在 `SYNC_STEPS` 数组中，参照 `sp_negative_targets` 和 `sb_negative_targets` 的结构，添加一个新的同步步骤对象。
  3.  **步骤配置**:
      - `id`: `'sd_negative_targets'`
      - `name`: `'SD否定商品定位'`
      - `tier`: `'high'` (与其它否定词同步层级保持一致，确保高频同步)
      - `execute`: `async (service, ctx) => { const result = await service.syncSdNegativeTargets(); return { success: true, synced: result.synced, errors: [] }; }` (并处理异常情况)。
- **验证**: 部署后，检查SD广告活动的否定定位数据是否能成功同步到数据库。

#### 3.1.2. 调整Node.js内存限制
- **目标**: 避免因内存溢出导致的服务崩溃。
- **实施步骤**:
  1.  **编辑项目根目录下的 `Procfile` 文件**。
  2.  将 `web` 命令从 `node --max-old-space-size=2048 ...` 修改为 `node --max-old-space-size=1400 ...`。
  3.  **说明**: 为 `t3.small` (2GB) 实例设置1400MB的堆内存上限，为操作系统和其他进程预留约600MB的内存空间，是生产环境中的最佳实践。
- **验证**: 部署后，通过CloudWatch监控实例内存使用率，确保其稳定在80%以下，不再出现因OOM (Out of Memory) 导致的崩溃。

### 3.2. P2 级问题修复 (中等优先级)

#### 3.2.1. 升级Elastic Beanstalk实例类型
- **目标**: 提升服务器计算和内存容量，以承载200-500租户规模的负载。
- **实施步骤**:
  1.  **登录 AWS Management Console**，导航至 Elastic Beanstalk 环境配置页面。
  2.  在“容量”或“实例”配置部分，将实例类型从 `t3.small` 升级到 `t3.medium` (4GB RAM) 或 `t3.large` (8GB RAM)。
  3.  **推荐**: 鉴于目标是支持高达500个租户，建议直接升级到 **`t3.large`**，为未来的业务增长提供充足的资源冗余。
  4.  **同步调整内存限制**: 如果升级到 `t3.large` (8GB)，可将 `Procfile` 中的 `--max-old-space-size` 调整为 `6144` (6GB)，以充分利用可用内存。
- **验证**: 升级后，在高峰时段监控CPU利用率、内存使用率和任务处理队列长度，确保系统在高负载下依然响应迅速。

#### 3.2.2. 修复“纠错监控”页面
- **目标**: 恢复“纠错监控”页面的正常功能。
- **实施步骤**:
  1.  **排查后端 `correction.ts` 路由**：重点审查 `getDashboard` 过程。该过程依赖的多条SQL查询均已正确添加了基于 `ctx.user.id` 的多租户数据隔离逻辑 (`accountFilter`)。
  2.  **分析问题**: 问题很可能出在前端对返回数据的处理上，或特定用户的某些数据格式异常导致渲染失败。
  3.  **修复策略**: 
      - **前端加固**: 在 `AutoCorrectionDashboard.tsx` 组件中，为所有从 `dashboardQuery.data` 中获取的数据添加空值或异常格式的保护性处理。例如，使用可选链 (`?.`)、空值合并运算符 (`??`) 和 `try-catch` 块来包裹数据处理逻辑。
      - **后端日志**: 在 `getDashboard` 过程中，为每个SQL查询的结果增加详细日志，以便在生产环境中追踪特定用户的数据问题。
- **验证**: 部署后，使用测试账号和生产账号登录，确认“纠错监控”页面能够稳定加载并正确显示数据。

### 3.3. P3 级问题优化 (低优先级)

#### 3.3.1. 优化并发配置
- **目标**: 在实例升级后，充分利用硬件资源，提升系统吞吐量。
- **实施步骤**:
  1.  **编辑 `.ebextensions/01_env.config` 文件**。
  2.  将 `MAX_CONCURRENT_ACCOUNTS` 环境变量的值从 `10` 提升到 `20` 或 `25` (配合`t3.large`实例)。
  3.  **编辑 `server/db/connection.ts` 文件**。
  4.  将 `DB_POOL_SIZE` 的默认值从 `25` 提升到 `50` 或 `60`。
  5.  **说明**: 这些调整将允许系统同时处理更多的账户同步任务，并为这些任务提供更多的数据库连接，从而显著缩短大规模租户的整体同步周期。
- **验证**: 部署后，监控 `sync_tasks_v2` 表中的任务完成时间，确认整体同步效率得到提升。

## 4. 实施与部署计划

1.  **版本创建**: 创建 `v389` 功能分支。
2.  **代码实施**: 按照 P1 > P2 > P3 的优先级顺序，逐一完成上述代码修改。
3.  **代码审查 (CR)**: 所有变更提交后，进行严格的代码审查。
4.  **构建与部署**: 合并到主分支，触发CI/CD流程，将 `v389` 版本部署到生产环境 (`amazon-ads-env-prod`)。
5.  **部署后验证**: 
    - 登录系统，验证“纠错监控”页面是否修复。
    - 检查SD广告活动的同步日志，确认 `sd_negative_targets` 步骤已执行。
    - 监控AWS CloudWatch指标，确认内存和CPU使用情况健康。
6.  **启动新一轮评估**: 在v389稳定运行24小时后，开始新一轮的全面系统排查，为v390版本的迭代做准备。
